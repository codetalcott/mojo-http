"""m0: the mojo-http framework's source, and the CLI that builds against it.

`__version__` lives here and nowhere else; the wheel's build reads it
(packaging/m0/hatch_version.py).
"""

__version__ = "0.3.0"
