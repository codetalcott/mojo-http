{
  "note": "Generated from docs/SPEC.md by scripts/spec_sheet.py. `verified` means a named CI gate exercises the capability on the stated cadence, not that it is correct.",
  "capabilities": [
    {
      "id": "A1",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "Persistent connections (keep-alive)",
      "status": "verified",
      "evidence": "`Smoke test pipelined requests` (every PR)"
    },
    {
      "id": "A2",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "The example Mojo server starts and answers `/health`",
      "status": "verified",
      "evidence": "`Smoke test the hello server` (every PR)"
    },
    {
      "id": "A3",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "Keep-alive request cap (`--max-keepalive-requests` / `M0_MAX_KEEPALIVE_REQUESTS`; 0 = never)",
      "status": "verified",
      "evidence": "`Smoke test the keep-alive request cap` (every PR) \u2014 closes on the cap request, and a stream or WebSocket upgrade landing there survives it; the probe's third phase refuses to pass on a build whose cap never fires. The smoke pins the cap at 100 explicitly, so the gate is independent of the default. No flag or env var exposes the limit"
    },
    {
      "id": "A4",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "Idle connection timeout, `--idle-timeout`",
      "status": "verified",
      "evidence": "`Smoke test the idle connection timeout` (every PR) \u2014 an answered keep-alive connection left quiet is closed at the deadline and no earlier, and one kept busy across it is not"
    },
    {
      "id": "A5",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "Header read timeout (slowloris defence)",
      "status": "verified",
      "evidence": "`Smoke test the header read timeout` (every PR)"
    },
    {
      "id": "A6",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "Request pipelining, answered in order (RFC 9112 \u00a79.3)",
      "status": "verified",
      "evidence": "`Smoke test pipelined requests` (every PR)"
    },
    {
      "id": "A7",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "Chunked request bodies, decoded incrementally across reads",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_incremental_decode_matches_a_single_pass` (every PR)"
    },
    {
      "id": "A8",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "Chunked decode consumes the terminator, leaving nothing buffered",
      "status": "verified",
      "evidence": "`test_chunked_encode.mojo:test_round_trip_single_chunk` (every PR)"
    },
    {
      "id": "A9",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "Chunked response bodies",
      "status": "verified",
      "evidence": "`Smoke test streamed WSGI bodies` (every PR)"
    },
    {
      "id": "A10",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "Trailer fields consumed and discarded, not surfaced to the application",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_a_trailer_section_is_consumed_whole` (every PR) \u2014 with seven more beside it: the framing fields a trailer must not honour, the pipelined tail surviving it, the section bounded by the abuse ratio, and `test_without_consume_trailer_the_body_ends_at_the_zero_chunk` as the other half. `poe sabotage-trailers` reverts each of the six rules and requires a failure for every one"
    },
    {
      "id": "A11",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "`Expect: 100-continue`, honoured case-insensitively and withheld from HTTP/1.0",
      "status": "verified",
      "evidence": "`Expect 100-continue, honoured and withheld` (every PR) \u2014 both directions: a server answering it unconditionally would pass the invitation half while discarding the refusal-before-upload the mechanism exists for"
    },
    {
      "id": "A12",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "Half-close answered rather than dropped",
      "status": "verified",
      "evidence": "`Smoke test a half-closed client` (every PR)"
    },
    {
      "id": "A13",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "Request headers larger than one socket read",
      "status": "verified",
      "evidence": "`Smoke test a request larger than one read` (every PR)"
    },
    {
      "id": "A14",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "`Host` required on HTTP/1.1",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_http11_requires_a_non_empty_host` (every PR)"
    },
    {
      "id": "A15",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "absolute-form request target reduced to its path",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_absolute_form_target_is_reduced_to_its_path` (every PR)"
    },
    {
      "id": "A16",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "Malformed field lines rejected, not treated as incomplete",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_a_field_line_without_a_colon_is_invalid_not_incomplete` (every PR)"
    },
    {
      "id": "A17",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "Control bytes in the request target rejected",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_a_control_byte_in_the_request_target_is_invalid` (every PR)"
    },
    {
      "id": "A18",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "HTTP/2",
      "status": "out of scope",
      "evidence": "terminate at a proxy \u2014 gunicorn's answer, and the same one applies here"
    },
    {
      "id": "A19",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "HTTP/3 and QUIC",
      "status": "out of scope",
      "evidence": "follows HTTP/2; there is no TLS layer to build it on"
    },
    {
      "id": "A20",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "A refusal sent mid-upload reaches the client: lingering close (RFC 9112 \u00a79.6)",
      "status": "verified",
      "evidence": "`Smoke test a 413 reaching a client still uploading` (every PR) \u2014 the whole body written before reading, `Content-Length` and chunked, through `http.client`, and eight at once; `Connection: close` and a FIN right behind the 413; the linger bounded against a client that keeps trickling past it, and a clean close at its deadline for a client that reads late"
    },
    {
      "id": "A21",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "No `Content-Length` on a 1xx or 204 response, and none invented on a 304 (RFC 9110 \u00a78.6)",
      "status": "verified",
      "evidence": "`test_response_head.mojo:test_a_204_invents_no_content_length_or_type` (every PR) \u2014 with six more beside it: the constructors invent neither a length nor a type for a 1xx, 204 or 304, and a 304 keeps a length its handler gave; `enforce_bodiless_framing`, which the event loop applies to every such response on every path, drops a 1xx's or 204's own length and any body (Django's `CommonMiddleware` puts a length on every non-streaming response) and closes a file body. On the wire, `Smoke test the notes API` checks the native 304s, the OPTIONS preflight and a delete's 204 for an entity header nobody set, and both conformance steps send a 204 carrying a length and seven bytes followed by a request on the same connection: the only case that shows whether the loop calls the rule. Before the fix every native 204 and 304 carried `content-length: 0` and `application/octet-stream`"
    },
    {
      "id": "A22",
      "section": "A",
      "category": "A. HTTP/1.1 framing and connection lifecycle",
      "capability": "An unencoded `@` in a request target's path or query is data: the request reaches its own route with its query, whatever its method, and only an `@` inside a URL's authority, before its first `/`, `?` or `#` (RFC 3986 \u00a73.2), ends a userinfo",
      "status": "verified",
      "evidence": "`test_uri_userinfo.mojo:test_an_at_in_a_request_query_keeps_the_path_and_query` (every PR) \u2014 `URI.parse` skipped to the first `@` anywhere after the scheme, so `GET /echo?x=a@b` reached the application as `/` with an empty query and a POST to `/save?x=a@b` ran the root's POST handler, on every server shape; the outbound client, which dials the parsed host, connected to whatever followed an `@` in a query. Browsers and htmx encode `@`, which kept it to typed and hand-built URLs; a Flask app compared against Werkzeug found it. Its neighbours hold the path form (`/users/@alice?page=2`), the client's host, and a real userinfo still skipped, and `Conformance test the WSGI bridge` sends a GET and a POST carrying the raw byte to a live server and requires the path, the query and the method back. The `://` half of the same search is `test_uri_scheme.mojo`"
    },
    {
      "id": "B1",
      "section": "B",
      "category": "B. Request smuggling (CWE-444)",
      "capability": "`Content-Length` with `Transfer-Encoding` rejected",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_content_length_with_transfer_encoding_is_rejected` (every PR)"
    },
    {
      "id": "B2",
      "section": "B",
      "category": "B. Request smuggling (CWE-444)",
      "capability": "Duplicate `Content-Length` rejected, including across letter case",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_duplicate_content_length_is_rejected_across_letter_case` (every PR)"
    },
    {
      "id": "B3",
      "section": "B",
      "category": "B. Request smuggling (CWE-444)",
      "capability": "`Transfer-Encoding` whose last coding is not `chunked` rejected",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_transfer_encoding_whose_last_coding_is_not_chunked_is_rejected` (every PR)"
    },
    {
      "id": "B4",
      "section": "B",
      "category": "B. Request smuggling (CWE-444)",
      "capability": "Header padding does not bypass the smuggling check",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_padded_headers_do_not_bypass_the_smuggling_check` (every PR)"
    },
    {
      "id": "B5",
      "section": "B",
      "category": "B. Request smuggling (CWE-444)",
      "capability": "Bare LF in a chunk extension rejected",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_bare_lf_in_a_chunk_extension_is_rejected` (every PR)"
    },
    {
      "id": "B6",
      "section": "B",
      "category": "B. Request smuggling (CWE-444)",
      "capability": "`Content-Length` integer overflow rejected",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_overflowing_content_length_is_rejected` (every PR)"
    },
    {
      "id": "B7",
      "section": "B",
      "category": "B. Request smuggling (CWE-444)",
      "capability": "Chunk size with the sign bit set rejected",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_chunk_size_with_the_sign_bit_set_is_rejected` (every PR)"
    },
    {
      "id": "B8",
      "section": "B",
      "category": "B. Request smuggling (CWE-444)",
      "capability": "h2spec conformance run",
      "status": "out of scope",
      "evidence": "follows from having no HTTP/2 (A18), the same reason C7 gives"
    },
    {
      "id": "B9",
      "section": "B",
      "category": "B. Request smuggling (CWE-444)",
      "capability": "PortSwigger-style desync scanning",
      "status": "out of scope",
      "evidence": "the scanner probes a proxy/server PAIR for disagreement about framing; this server has no proxy in front of it in any gate, so there is no second parser to disagree with. The shapes it looks for are unit-tested directly above (B1-B7), and fuzzing the decoder itself is G13"
    },
    {
      "id": "C1",
      "section": "C",
      "category": "C. Connection management and denial of service",
      "capability": "Max request body size, configurable",
      "status": "verified",
      "evidence": "`Smoke test the serve CLI` (every PR) \u2014 `--max-body`"
    },
    {
      "id": "C2",
      "section": "C",
      "category": "C. Connection management and denial of service",
      "capability": "Chunk size limited to 16 significant digits",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_chunk_size_is_limited_to_sixteen_significant_digits` (every PR)"
    },
    {
      "id": "C3",
      "section": "C",
      "category": "C. Connection management and denial of service",
      "capability": "Raw chunked bytes bounded independently of decoded size",
      "status": "verified",
      "evidence": "`Smoke test the serve CLI` (every PR) \u2014 a decoded-legal body costing 2x the cap in framing answers 413"
    },
    {
      "id": "C4",
      "section": "C",
      "category": "C. Connection management and denial of service",
      "capability": "Max header count",
      "status": "verified",
      "evidence": "`test_parsing.mojo:test_header_count_is_capped` (every PR)"
    },
    {
      "id": "C5",
      "section": "C",
      "category": "C. Connection management and denial of service",
      "capability": "A slow handler does not stall connections behind it",
      "status": "verified",
      "evidence": "`Smoke test the blocking-threads pool` (every PR)"
    },
    {
      "id": "C6",
      "section": "C",
      "category": "C. Connection management and denial of service",
      "capability": "Connection/request rate limiting",
      "status": "out of scope",
      "evidence": "a proxy's job; this server has no notion of a client identity to limit on"
    },
    {
      "id": "C7",
      "section": "C",
      "category": "C. Connection management and denial of service",
      "capability": "HTTP/2 reset-flood limits",
      "status": "out of scope",
      "evidence": "follows from having no HTTP/2"
    },
    {
      "id": "C8",
      "section": "C",
      "category": "C. Connection management and denial of service",
      "capability": "A burst of new connections does not hold the event loop away from the connections it already serves: one pass admits at most `ACCEPT_BATCH` (16) of them \u2014 off the listener, or off a sibling's accept-share channel \u2014 AFTER it has served the events of the connections it holds, and what a batch leaves is owed to the next pass, whose wait does not block, because both listeners are edge-triggered and nothing announces the same backlog twice; the graceful drain still admits every connection a sibling handed over, and the loop inversion takes owed batches inside its callback up to the old bound. `M0_ACCEPT_BATCH` overrides the batch, 0 taking the whole backlog in one pass as the loop used to (an A/B knob). Before: epoll reports no backlog depth, so its drain ran to EAGAIN, through connections that arrived during it, up to `max_connections` \u2014 and every admission is the connection's eager read, on a loop that runs `func` itself the whole request",
      "status": "verified",
      "evidence": "`Smoke test the accept batch` (every PR) \u2014 `apps/pool_spike` with `func` on the loop: a 600 ms request parks the loop, 120 connections queue behind it each carrying a 10 ms request, and a keep-alive connection already established sends `/fast`. No bound is in nominal milliseconds, because a runner's timers oversleep (two of CI's macOS runs failed one with the loop behaving). `/fast` must be answered within one and a half batches of what one such request COSTS on the box, measured first and counted from the blocker's own answer (the default 16, which the probe reads from the constant; 153 ms measured on Linux against about 256, the rest of the one batch the blocker itself was admitted in), with all 120 answered (a batch stranded behind the listener times out) and no two answers more than 500 ms apart: half the loop's one-second wait, which a wait that blocked between owed batches runs to (1012 ms measured), where the widest honest gap is one request (11 ms on Linux; 54\u2013100 ms on the macOS runner, whose per-request cost moved from 17 to 49 ms inside one run, so the burst's total is printed, not bounded). On Linux the same probe with `M0_ACCEPT_BATCH=0` must show the starvation, at least two thirds of the burst's cost (1225 ms measured against about 850) \u2014 the arm that proves the probe sees the old drain; on macOS kqueue reports the depth and the knob alone does not recreate the old order, so it is not asserted there. Sabotaged by hand before landing \u2014 no cap, nothing owed, a wait that blocks while a batch is owed, the batch taken where the listen event falls \u2014 each caught by its own check, recorded in the note with the throughput measured beside it. The design is [the-accept-batch](notes/the-accept-batch.md)"
    },
    {
      "id": "D1",
      "section": "D",
      "category": "D. Graceful shutdown and restart",
      "capability": "SIGTERM drains in-flight requests",
      "status": "verified",
      "evidence": "`Smoke test graceful shutdown` (every PR)"
    },
    {
      "id": "D2",
      "section": "D",
      "category": "D. Graceful shutdown and restart",
      "capability": "SIGTERM to the supervisor alone reaps workers",
      "status": "verified",
      "evidence": "`Smoke test graceful shutdown` (every PR)"
    },
    {
      "id": "D3",
      "section": "D",
      "category": "D. Graceful shutdown and restart",
      "capability": "Bounded drain, naming what it abandoned",
      "status": "verified",
      "evidence": "`Smoke test streamed WSGI bodies` (every PR)"
    },
    {
      "id": "D4",
      "section": "D",
      "category": "D. Graceful shutdown and restart",
      "capability": "SIGTERM reaches a shutdown pipe rather than killing the process",
      "status": "verified",
      "evidence": "`test_lifecycle.mojo:test_sigterm_reaches_the_shutdown_pipe` (every PR)"
    },
    {
      "id": "D5",
      "section": "D",
      "category": "D. Graceful shutdown and restart",
      "capability": "Development hot reload on file change",
      "status": "verified",
      "evidence": "`Smoke test hot reload` (every PR) \u2014 `--reload`, `--reload-dir`"
    },
    {
      "id": "D9",
      "section": "D",
      "category": "D. Graceful shutdown and restart",
      "capability": "A request whose body is still arriving at SIGTERM is read on and answered, not left to the deadline",
      "status": "verified",
      "evidence": "`Smoke test the drain with an upload in flight` (every PR) \u2014 `scripts/drain_upload_probe.py`, two-sided: answered whole AND exited inside 3 s, on the ASGI executor and on a WSGI pool thread"
    },
    {
      "id": "D10",
      "section": "D",
      "category": "D. Graceful shutdown and restart",
      "capability": "A worker that fails its drain \u2014 exits non-zero, or dies of a signal other than the one forwarded \u2014 after the supervisor was told to stop is not respawned; the supervisor exits 1 once the rest are gone, rather than serving a replacement nothing will ever signal",
      "status": "verified",
      "evidence": "`test_respawn.mojo:test_a_worker_that_fails_while_stopping_is_not_respawned` (every PR) \u2014 a supervisor signalled alone forwards SIGTERM, its worker exits 9 from the drain, and the test requires exit 1 and no second incarnation; with the guard reverted the worker is respawned and the supervisor exits 0. Found by `sabotage-host`: a host worker returning from `main` crashed on its way out, was respawned, and its supervisor outlived the smoke holding the port (the note: [the-mojo-host](notes/the-mojo-host.md)). The stop is recorded by the signal handler in a data-segment word beside the child PIDs (`src/global_slot.mojo`)"
    },
    {
      "id": "D11",
      "section": "D",
      "category": "D. Graceful shutdown and restart",
      "capability": "An ASGI application's work still running at shutdown is bounded: background work gets the drain's 3 s grace, is then cancelled and named, and lifespan shutdown still runs",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR) \u2014 a response whose background task never ends, then SIGTERM: the process must exit and write the lifespan shutdown marker. Before, the post-pill gather waited on every HTTP task, the 5 s thread join gave up, and the process left without shutting the application down (exit 0, no marker). `poe test-shim` holds the rest: work inside the grace finishes, a task that swallows its cancellation is left behind after half a second and never reaches the executor again, and the drain starts once"
    },
    {
      "id": "D6",
      "section": "D",
      "category": "D. Graceful shutdown and restart",
      "capability": "`SO_REUSEPORT` off by default: a second bind fails loudly instead of silently sharing the port",
      "status": "verified",
      "evidence": "`Smoke test the serve CLI` (every PR) \u2014 five attempts a second apart, so a restart racing the previous process's drain still succeeds; the second server must exit 1 naming the address, print no ready banner, and leave the first answering. The opt-in `ListenConfig.reuse_port` remains for a deliberate handoff between two processes that both mean to listen; no shipped path enables it, because workers and threads all accept from ONE listener bound before the fork"
    },
    {
      "id": "D7",
      "section": "D",
      "category": "D. Graceful shutdown and restart",
      "capability": "Binary/hot upgrade (USR2-style overlap)",
      "status": "out of scope",
      "evidence": "needs socket handoff this server does not have; run two behind a proxy"
    },
    {
      "id": "D8",
      "section": "D",
      "category": "D. Graceful shutdown and restart",
      "capability": "SIGHUP reload",
      "status": "out of scope",
      "evidence": "`--reload` covers development; production reload is a new process behind a proxy"
    },
    {
      "id": "E1",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "Multi-process prefork with a supervising parent",
      "status": "verified",
      "evidence": "`Smoke test the Django WSGI example` (every PR)"
    },
    {
      "id": "E2",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "Crashed workers respawned",
      "status": "verified",
      "evidence": "`test_respawn.mojo:test_respawned_worker_returns_to_the_callers_startup_path` (every PR)"
    },
    {
      "id": "E3",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "A spent respawn budget exits nonzero rather than looping",
      "status": "verified",
      "evidence": "`test_respawn.mojo:test_supervisor_exits_nonzero_when_respawn_budget_is_spent` (every PR)"
    },
    {
      "id": "E4",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "Handler thread pool behind each event loop",
      "status": "verified",
      "evidence": "`Smoke test the Mojo handler pool` (every PR)"
    },
    {
      "id": "E5",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "Free-threaded CPython, N loops on N threads",
      "status": "verified",
      "evidence": "`py-canary` (weekly)"
    },
    {
      "id": "E6",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "A GIL-enabled interpreter is refused, never warned-and-run",
      "status": "verified",
      "evidence": "`Smoke test the threaded mode's guard` (every PR)"
    },
    {
      "id": "E7",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "Zero-config topology defaults",
      "status": "verified",
      "evidence": "`Smoke test --doctor against the server's own exit codes` (every PR)"
    },
    {
      "id": "E8",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "Max-requests worker recycling with jitter",
      "status": "out of scope",
      "evidence": "the leak it mitigates is measured instead \u2014 `smoke-django` fails on RSS growth over 10k requests"
    },
    {
      "id": "E10",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "A worker's configuration refusal (exit 78) ends supervision with 78, never a respawn loop",
      "status": "verified",
      "evidence": "`test_respawn.mojo:test_a_worker_refusing_its_configuration_is_not_respawned` (every PR)"
    },
    {
      "id": "E11",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "The event loop holds no thread state while it serves; a pool thread that drops the GIL hands it to the thread parked waiting for it",
      "status": "verified",
      "evidence": "`probe-pool-fairness` (pre-release) \u2014 four pool threads under a CPU-bound view at sixteen connections must hold a single-digit-millisecond fast-route p99 and a max under a quarter second, and the same run with the barrier disabled must show the convoy (a max of seconds), which is what proves the probe can see the failure it guards. On Linux a third arm runs with E34's rule off and must starve a waiter past those bounds: before the rule the fair arm itself failed there 14 of 14 runs (max 335\u20131637 ms on a 4-vCPU VM, found by the 2026-09-26 pre-release run), 12.9\u201320.6 ms with it. The throughput half of the change is the detached-loop A/B in docs/notes/detached-loop.md"
    },
    {
      "id": "E34",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "Inside its hand-off slice a pool thread takes a job already queued on its lane without dropping the GIL (`OffloadPool.try_next_job`: `next_job`'s pill check, socket poll and ring pop, never its spin or park), so the only drops a slice makes are its hand-off and the view's own. Each drop between jobs used to wake a parked waiter that found the GIL taken again and queued behind the others, so the slice's hand-off went back to the thread that had just held it: two threads alternating while two starved, 0.3\u20131.6 s at a time on 4-vCPU Linux under a CPU-bound view. Only with the barrier on (a pool of one and `M0_POOL_TURN=0` are unchanged); `M0_POOL_TURN_KEEP=0` drops the GIL between every job again (an A/B knob)",
      "status": "verified",
      "evidence": "`test_blocking_pool.mojo:test_a_slice_takes_the_jobs_queued_behind_it_without_dropping_the_gil` (every PR) \u2014 64 jobs through four threads whose views hold the GIL, every thread awake: all answered, some taken inside a slice, none under the knob (on a free-threaded interpreter, where nothing waits for a GIL, only the answers). Beside it in `test_offload.mojo`: `test_try_next_job_takes_what_is_queued`, `test_try_next_job_answers_an_empty_lane_at_once` (on a thread of its own, so a version that waits fails inside a second rather than hanging the suite) and `test_try_next_job_answers_the_pill`. The fairness itself is E11's probe, whose Linux arm runs with the knob. Each rule sabotaged by hand and caught by its own test. The design and the measurements: [a-slice-keeps-the-gil](notes/a-slice-keeps-the-gil.md)"
    },
    {
      "id": "E9",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "Worker lifetime / max-RSS recycling",
      "status": "out of scope",
      "evidence": "same reason as above"
    },
    {
      "id": "E12",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "The OS multiplexer is chosen in one place: `lightbug_http/c/platform.mojo` names `PlatformBackend` (kqueue on macOS, epoll on Linux) and the two libc constants whose values differ between them, and no other file imports or constructs a backend",
      "status": "verified",
      "evidence": "`test_platform.mojo:test_platform_backend_is_a_constructible_multiplexer` (every PR) \u2014 the alias constructs through the trait-declared initializer and owns a multiplexer fd on the running OS. The other half, that nothing else chooses a backend, is `check_backend_seam` in `scripts/check_docs.py`, a rule over the tree's text that `check-docs` runs on every pull request"
    },
    {
      "id": "E13",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "`--doctor` reports the performance-core count beside the logical count (`topology.performance_cpus`: `hw.perflevel0.logicalcpu` on Apple Silicon, equal to `cpus` on a machine with one performance level) and `build.apple_target`, the generation the binary was compiled for",
      "status": "verified",
      "evidence": "`Smoke test Apple Silicon topology, QoS and the native build flavor` (every PR) \u2014 the count is checked against `sysctl`, a second source that mirrors the stdlib's fallback, and the wheel flavor must answer m1 on whatever Mac runs it, because `build-serve` pins its target to the oldest Apple Silicon"
    },
    {
      "id": "E14",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "`--qos` / `M0_QOS`: on macOS the event loop runs at user-interactive QoS and pool and executor threads at user-initiated, the one placement lever Darwin offers over performance and efficiency cores; accepted and ignored elsewhere",
      "status": "verified",
      "evidence": "`Smoke test Apple Silicon topology, QoS and the native build flavor` (every PR) \u2014 the doctor reports the knob from both spellings, and a server under it answers a loop-answered route and a pool-thread view. The effect is measured rather than smoked: docs/notes/desktop-mac-server.md"
    },
    {
      "id": "E15",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "`--spawn-workers` / `M0_SPAWN_WORKERS`: each worker is forked and at once execs the binary afresh, inheriting the listener, the bus channels and a file-backed shared page by fd, so an application may use what a forked child cannot (Core ML, Objective-C, CoreFoundation, libdispatch); the same supervisor, respawn and signals",
      "status": "verified",
      "evidence": "`Smoke test spawned workers` (every PR) \u2014 two exec'd workers serve one listener, a SIGKILLed one is re-exec'd, SIGTERM to the supervisor reaps them, and the doctor reports `worker_mode`; on macOS the negative arm: `urllib.request.getproxies()` answers under spawn and kills a forked worker 3 of 3. The exec path itself: `test_respawn.mojo:test_spawned_workers_run_the_exec_image_with_their_index`"
    },
    {
      "id": "E17",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "The zero-config pool runs one handler thread until a job has waited: only one idle thread per lane spins, a push wakes nobody while a thread of the lane is busy or spinning, a ring that holds a job and whose pop count has not moved for `POOL_WAKE_AGE_NS` has a parked sibling woken by the loop (a ring being drained is left alone, however deep \u2014 on a GIL build; without a GIL the pool is parallel, `set_parallel`: a push wakes a parked thread whenever there is one and the wait counts from the push), and every thread is woken by name on a channel of its own, most recently parked first (`M0_POOL_ELASTIC=0` restores the eager wakes)",
      "status": "verified",
      "evidence": "`test_offload.mojo:test_a_job_behind_a_slow_sibling_is_taken_once_the_loop_wakes_a_parked_one` (every PR) \u2014 the rules beside it in the same file: `test_submit_wakes_nobody_while_a_sibling_is_busy`, `test_submit_wakes_nobody_while_a_sibling_spins`, `test_a_burst_into_a_parked_lane_wakes_one_thread`, `test_a_ring_being_drained_is_never_stalled`, `test_on_a_free_threaded_interpreter_the_pool_wakes_eagerly_and_counts_from_the_push`, `test_sequential_jobs_with_idle_gaps_stay_on_one_thread`, `test_a_wake_lands_on_the_parked_threads_own_channel`, and the knob-off arm `test_elastic_off_restores_the_chained_wake`; the isolation the rules must keep is C5's smoke. The measurement, both platforms: docs/notes/elastic-pool.md"
    },
    {
      "id": "E16",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "Workers sharing one listener share its connections: the worker that wins an accept passes the connection to the least-loaded sibling over a pre-fork `SCM_RIGHTS` channel, so a burst of keep-alive connections spreads across `--workers N` to within 2:1 on both platforms, forked or spawned, at no cost to a single worker (`M0_ACCEPT_SHARE=0` restores the bare race)",
      "status": "verified",
      "evidence": "`Smoke test accept sharing` (every PR) \u2014 two rounds of a 32-connection burst per worker mode must land within 2:1, the doctor must report `accept_sharing` for two workers and not for one, and on macOS the knob-off arm must skew; the descriptor passing and the load policy: `test_accept_share.mojo:test_a_connection_passes_between_two_workers_with_its_peer`. The measurement, both platforms before and after: docs/notes/accept-sharing.md"
    },
    {
      "id": "E18",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "A job pushed to a pool lane whose threads are parked or inside slow views is taken without a lost wake, on Linux, where the kernel wakes one waiter and the only lost pool wake ever caught reproduced",
      "status": "verified",
      "evidence": "`stress-pool` (pre-release) \u2014 in the `m0lin` container, once per shipped wake mode (the GIL rules, `M0_POOL_PARALLEL=1`, `M0_POOL_ELASTIC=0`): `scripts/probes/phase5_probe.py` runs `smoke-django-realtime` phase 5 twenty times with a fresh server each round \u2014 two 1.5 s views in flight, a hold taken on a pool thread, its subscriber must be registered half a second later; 2 of 10 rounds failed on the ring's first build (docs/notes/pool-ring-handoff.md) \u2014 and `scripts/probes/hold_race_probe.py` opens forty holds while three clients keep the loop busy. The deterministic half is E17's unit tests; the isolation is C5's smoke"
    },
    {
      "id": "E19",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "A view that RELEASES the GIL runs in parallel across `--blocking-threads`, and one that holds it does not \u2014 the pool's N threads do N things at once only when the work inside the view drops the interpreter lock",
      "status": "verified",
      "evidence": "`Smoke test pool parallelism` (every PR) \u2014 `bareapp`'s `/work` is one hashlib call over two buffer sizes either side of `HASHLIB_GIL_MINSIZE`, so the lock is the only variable: two at once cost 1.02x one for the releasing mode and 2.01x for the holding one, and a server given a single handler thread sends both to 2.0x. C5 is the isolation half of the same pool; this is the concurrency half, which C5 passes without. What it decides in practice: Core ML's `predict` holds the lock and an embedding app gained nothing from more handler threads, MAX releases it and the same app scaled \u2014 docs/notes/gil-and-the-handler-pool.md"
    },
    {
      "id": "E20",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "With no handler pool \u2014 the loop calling a WSGI application itself, as `--blocking-threads 0` (with or without `--realtime`) and every `--workers N` without a pool do \u2014 the loop releases the GIL around each wait for I/O, so a thread the application starts runs while the server is idle, as it does under a pool, gunicorn or uvicorn",
      "status": "verified",
      "evidence": "`Smoke test application threads without a pool` (every PR) \u2014 `bareapp.ticker`'s thread sleeps 10 ms a tick; over 3 s idle on one keep-alive connection it must tick 20 times in each of the three shapes, whose banners must name no pool \u2014 a bar set from GitHub's shared macOS runner, which measured 19 to 41 ticks over 1.5 s (nominal 150) across eleven shape-runs, not from nominal. The loop used to wait holding the GIL it has held since `Py_Initialize`, and the thread ran only when a request ran Python: 1 tick where 150 were due, in all three (#310). E11 is the pooled shape, which detaches by a different route"
    },
    {
      "id": "E21",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "The Mojo host: `m0_host.serve[H, P]` owns a Mojo application's startup \u2014 the listener, the pre-fork shared pages and bus, the fork, accept sharing bound per worker, signals armed after the fork, a handler built in each worker by `AppHandler.make`, and one `Producer` on worker 0 alone \u2014 so the bus is drained at one worker too, and every worker's streams receive every step of the one producer, in order and once; a configuration it does not serve (`M0_SPAWN_WORKERS`, `M0_WORKERS=0`; `M0_BLOCKING_THREADS` too until E26 served it, and `M0_THREADS` until E27) exits 78, naming the variable, before anything is bound",
      "status": "verified",
      "evidence": "`Smoke test the Mojo host` (every PR) \u2014 `apps/host_check` is a handler and a producer and nothing else: a stream at one worker must beat, four streams that must span two workers (a run that cannot spread them fails as vacuous) must each carry every beat with no id repeated or skipped and every beat from one process (the pid in the payload: the loop's redelivery filter hides a second producer's repeated ids, which the first sabotage run missed), and each refusal must exit 78 without the listening banner; the thread half is `test_host.mojo:test_producer_publishes_to_every_worker_in_order`. `sabotage-host` (pre-release) breaks twenty-five host rules one at a time \u2014 worker 0's channel skipped, one channel handed to the producer, the bus drained only above one worker, a producer in every worker, the share never bound, signals armed before the fork, a forked worker returning from `main`, the refusal skipped or placed after the bind, the thread rules under E23 and the pre-fork rules under E24 \u2014 and requires the gate to fail for each. The design and what the host does not do: [the-mojo-host](notes/the-mojo-host.md)"
    },
    {
      "id": "E26",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "The Mojo host serves `M0_BLOCKING_THREADS=N` as a handler pool behind each worker's loop: N `MojoPool` threads on one lane the host marks GIL-free, each building the application's handler again with `AppHandler.make` on its own thread (`PoolLane[H]`, `HostContext.thread` naming the instance), so a trivial route answered on the loop stays answered while every pool thread is inside a slow view, and the same load on the bare loop stalls it; a stream opened on the loop (`before_request`, or `on_loop=True` on a table) works under the pool at one worker and two, while one begun in `func` on a pool thread is refused 409 and named in the log, as on a Mojo mount; a `make` that raises on a pool thread is the same 78 the loop's gets, before the server takes a connection, never a pool one thread short; and the shutdown bounds overlap \u2014 the loop stamps the producer's stop word as its drain begins, and the pool and producer joins count from that stamp \u2014 so a request in flight at SIGTERM beside a step past its bound leaves inside one 5 s bound, not two",
      "status": "verified",
      "evidence": "`Smoke test the Mojo host` (every PR) \u2014 `apps/host_check` under `M0_BLOCKING_THREADS=2`: two connections looping a 200 ms spin hold BOTH threads (with a thread to spare an idle thread answers a job within a millisecond and the phase could not tell it from the loop), and the worst of 24 `/health` samples (answered in `before_request`) under 100 ms (0 ms measured on an M4), where `M0_BLOCKING_THREADS=0` under the same load must reach 100 ms (389 ms measured) or the arm proves nothing; two streams opened in `before_request` under that pool carry every beat, and four spanning two pooled workers do; `/events-from-func` answered 409 with the refusal in the log; `M0_HOSTCHECK_POOL_MAKE_RAISES=1` exiting 78 at one worker and two, naming the knob, with no worker left; and a 4 s request in flight at SIGTERM beside a 60 s step answered whole with the process gone in 6 s (5.05 s measured; 8.54 s with the stop word unstamped, the two bounds in sequence). The unit halves are `test_host.mojo:test_a_pool_lane_builds_the_handler_for_its_thread`, `test_a_pool_lane_reports_a_make_that_raised` and `test_a_stop_stamped_by_the_loop_shortens_the_join`, and `test_mojo_pool.mojo:test_wait_ready_counts_a_thread_whose_make_raised`. `sabotage-host` (pre-release) serves the variable on the loop, withholds the stop word from the loop, counts the producer's join from the loop's return, serves a raising pool `make` one thread short, builds a pool thread's handler as the loop's own, and moves the gate app's `/health` onto the pool. The design: [the-ramp-test](notes/the-ramp-test.md)"
    },
    {
      "id": "E22",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "A Mojo host application under `M0_WORKERS=2` shares its accepts as `m0serve` does (E16): a burst of keep-alive connections lands within 2:1, with connections actually passed between workers, and `M0_ACCEPT_SHARE=0` restores the bare race",
      "status": "verified",
      "evidence": "`Smoke test the Mojo host` (every PR) \u2014 `scripts/accept_spread.py --app-bin` runs E16's measurement on `apps/host_check`: two rounds of a 32-connection burst within 2:1 and a non-zero hand-off count, and on macOS the knob-off arm must skew (measured 16/16 against 27/5 on an M4). No app under `apps/` shared its accepts before the host"
    },
    {
      "id": "E23",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "The host's producer ends with the server: told to stop as the drain begins and joined within what the drain left of the 5 s bound (E26), its sleeps sliced so a long period never delays the stop; a step still running at the bound is abandoned by name and the process exits 0, in one process and in a forked worker alike; a drain signalled to the supervisor alone ends every held stream and both workers report a clean exit; the cadence never catches up after an overrun, and a step that raises ends the producer with its status",
      "status": "verified",
      "evidence": "`Smoke test the Mojo host` (every PR) \u2014 a 60 s step must leave 5 s after SIGTERM with the producer named, a 60 s period must leave at once, and the supervisor-only drain must end four held streams across two workers with no worker killed by the signal; the thread rules without a server are `test_host.mojo:test_an_overrunning_step_is_abandoned_within_the_bound` and its neighbours (no catch-up, proven by one overrun followed by free steps, since steps that all overrun run back to back either way; a prompt stop; a raising step's status; a counted refusal). `sabotage-host` (pre-release) removes the stop, the bound, the `_exit`, the sliced sleep and the status-last write, among others"
    },
    {
      "id": "E24",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "One pre-fork preparation serves both hosts: `m0_http.prefork` makes the shared page (file-backed where the host allows it, the magic word stored, `M0_SHARED_ID_FD` and `M0_SHARED_ID_ADDR` exported), the bus and the accept-share channels for `m0serve` and for the Mojo host alike, each exported by descriptor number; a spawned worker adopts all three by those numbers, the page mapped again in its own image and its address re-exported before anything reads it, and a spawned worker handed no page descriptor is refused rather than served from an address that is not its own",
      "status": "verified",
      "evidence": "`test_prefork.mojo:test_a_spawned_worker_adopts_the_page` (every PR) \u2014 a page made with `required=True` and stored through, then adopted under the spawn marker, must sit at a new address, be the address `shared_id_addr()` reports, carry the store and the magic word, and a store through the adopted mapping must reach the original; its neighbours cover the bus and the share adopted by descriptor, the one-worker and `M0_ACCEPT_SHARE=0` cases, and the missing-descriptor refusal. The exec itself is E15, which runs on the same functions. `sabotage-host` (pre-release) breaks six of its rules against that file \u2014 the adopt paths replaced by fresh pages and channels, the address not re-exported, the magic word and the send ends not stored, the knob ignored"
    },
    {
      "id": "E25",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "The host's contract holds through its own supervisor: a producer's event ids come from the pre-fork shared word (`Publisher.next_id`), the one the handlers' own publishes number from, so a respawned worker 0's producer continues above what every held stream has seen rather than restarting at 1 below it and leaving those streams silent for the pre-crash uptime; and a `make` that raises \u2014 the handler's in any worker, the producer's on worker 0, built on the spawning thread before the server listens \u2014 is a refusal, exit 78 with the error named under `host:`, never a crash loop, and one worker's refusal ends its siblings rather than leaving them serving a host missing the worker that refused",
      "status": "verified",
      "evidence": "`Smoke test the Mojo host` (every PR) \u2014 four streams spanning two workers are held through a SIGKILL of worker 0, and those on worker 1 must beat again within 2 s of the kill, from the respawned producer, with an id above their last (12 ms measured; before the fix no beat arrived inside the bound after 41 beats over 4 s, the silence being the pre-kill uptime) \u2014 a fresh stream beating is no evidence, since that passes on the broken host; and `M0_HOSTCHECK_MAKE_RAISES=1` and `M0_HOSTCHECK_PRODUCER_RAISES=1`, at one worker and two, must each exit 78 naming the knob, with no respawn or crash line and no worker left. The thread halves are `test_host.mojo:test_publishers_number_from_the_one_shared_word` (two publishers over one word, a handler's take between them) and `test_a_raising_make_starts_no_thread` (`STATUS_NEVER_RAN`, distinct from a raising step's `STATUS_RAISED`); the supervisor's half is `test_respawn.mojo:test_a_refusal_by_one_worker_ends_its_siblings`. `sabotage-host` (pre-release) creates the id word after the fork, stops `next_id` advancing it, lets the handler's raise propagate, exits 1 for the producer's, swallows it inside `start`, and leaves the siblings serving"
    },
    {
      "id": "E27",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "The Mojo host serves `M0_THREADS=N` as N event loops on N threads of ONE process: the listener, the shared page, the bus and the accept-share channels are made once and sized by the loop count, each thread builds its own handler with `AppHandler.make` (`HostContext.worker` naming the loop, `threaded` the kind), and fan-out across loops rides the bus, one channel per loop, from one producer thread \u2014 so every loop's streams carry every beat; accept sharing runs across the threads unchanged, a passed descriptor arriving in the same process; and `M0_BLOCKING_THREADS` composes as T loops of N",
      "status": "verified",
      "evidence": "`Smoke test the Mojo host's loops on threads` (every PR) \u2014 `apps/host_check` under `M0_THREADS=2`: the banner names two loops and no worker was forked; four streams that must SPAN both loops (`x-worker`, or the phase fails as vacuous) each carry every beat, contiguous, from one pid; and under `M0_BLOCKING_THREADS=2` as well, each loop starts its own pool, the four streams still beat, and the worst of 24 `/health` samples behind two spinning connections stays under 100 ms. There is no interpreter in a Mojo host, so nothing here skips on CI's GIL-enabled CPython, unlike B-section's threaded rows. `sabotage-host` (pre-release) drains only loop 0's bus channel and builds every loop's handler as loop 0. Measured on macOS without accept sharing: 30 of 32 connections on one loop of four; with it, 8 each. The design and the measurement against prefork: [loops-on-threads](notes/loops-on-threads.md)"
    },
    {
      "id": "E28",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "A threaded Mojo host drains as one process: the signal handler's one pipe wakes the spawning thread, which pokes a shutdown pipe per loop, every loop finishes the request it holds and leaves, its pool joined within what the drain left of the bound, and `main` returns 0 \u2014 nothing was forked, so there is no `exit_worker`; a loop still running past its bounds is named and the exit is 1; and a loop that raises takes the process, there being no supervisor to replace it",
      "status": "verified",
      "evidence": "`Smoke test the Mojo host's loops on threads` (every PR) \u2014 a 1.5 s `/slow` request written on one connection per loop (the probe first proves the connections sit on different loops), SIGTERM while both spin: both answered 200 whole, exit 0 inside 8 s (1 s measured), no loop or producer reported left behind. `sabotage-host` (pre-release) hands only loop 0 its shutdown pipe: loop 1 never sees the stop, the coordinator's bounded join gives up on it and the exit is 1"
    },
    {
      "id": "E29",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "Under `M0_THREADS` every loop's handler is its own instance, and an application whose state lives in one handler is refused rather than served as N copies: `AppHandler.max_threads()` defaults to `max_workers()`, so the application that refused workers refuses loops without being asked (`ViewsApp` forwards it), and one whose state really is shared across loops overrides it; `M0_WORKERS>1` with `M0_THREADS>1` is refused, as is `M0_THREADS=0`; each with 78 before the bind; and no loop takes a connection until EVERY loop has its handler, so a `make` that raises on any loop is a 78 before anything was served",
      "status": "verified",
      "evidence": "`Smoke test the Mojo host's loops on threads` (every PR) \u2014 five `/instance` requests on keep-alive connections spanning both loops: one pid, and each loop's handler has counted exactly its own requests, 1..n; `M0_THREADS=2` with `M0_HOSTCHECK_MAX_WORKERS=1`, with `M0_WORKERS=2`, and `M0_THREADS=0` each exit 78 naming the reason with no `listening` line; `M0_HOSTCHECK_LOOP_MAKE_RAISES=1` raises on loop 1 alone, 300 ms late, and exits 78 with no `Event loop started` line. `Smoke test the fragment notes app` (every PR) refuses `M0_THREADS=2` for the notes app, which says only `max_workers() -> 1`. The unit halves are `test_host.mojo:test_the_host_refuses_more_loops_than_the_app_serves` and `test_workers_and_threads_are_one_or_the_other`. `sabotage-host` (pre-release) removes the barrier, the loop limit, the default, the conflict and `ViewsApp`'s forwarding"
    },
    {
      "id": "E30",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "A Mojo host application has a command line, and its precedence is m0serve's: flag over `M0_` variable over default. `serve` lays the flags over the `AppConfig` it is handed \u2014 `--host`, `--port`, `--workers`, `--threads`, `--blocking-threads`, `--access-log`, `--sse-heartbeat-ms`, `--app-tick-ms`, `--max-keepalive-requests`, `--qos`, `--spawn-workers` (read, and refused as its variable is), `--doctor` and `--help` \u2014 so `serve(AppConfig())` is still an application's whole `main`; a flag marks its count as chosen exactly as its variable does; a flag that names one of the four fields a `ServerConfig` shares outranks tuning the application set in code, and one that was not given leaves it alone; the overlay is idempotent, which is what lets an application that prints its own address take `host_config()` and be served the same. Strict where the environment is lenient: an unknown flag, a missing or unreadable value, a value on a boolean, a port outside 1\u201365535 or any positional is the usage and exit 2, before anything is bound \u2014 an application's own settings stay in its own variables",
      "status": "verified",
      "evidence": "`Smoke test the Mojo host's flags and doctor` (every PR) \u2014 `apps/host_check` answers `/health` on the port a flag names while `M0_PORT` names another, which nothing then answers on; an access-log flag logs the request; two workers and two pooled loops asked for by flag are what the banner and the log show; and the first two hold in BOTH application shapes, `serve(host_config())` and `serve(AppConfig())`, because the first applies the command line before `serve` sees it and hid whether `serve` does (two sabotages MISSED until the second shape was run). Six unreadable command lines each exit 2 with the usage and the reason, served and doctored alike, and `--help` exits 0 having started nothing. The unit halves are `test_host_flags.mojo:test_a_flag_outranks_its_variable_which_outranks_the_default` and `test_what_cannot_be_read_is_a_usage_error`, beside the idempotence, `*_set` and given-flag tests. `sabotage-host` (pre-release) serves the environment under a command line it read, withholds a given flag from the `ServerConfig`, prints the gate app's banner from the environment alone, ignores an unknown flag, leaves a count unmarked, leaves the base URL on the old port, and overwrites the application's tuning with a flag nobody gave. The design: [flags-and-a-doctor-for-the-host](notes/flags-and-a-doctor-for-the-host.md)"
    },
    {
      "id": "E31",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "A Mojo host application's doctor: the binary prints the configuration it WOULD serve as one JSON object \u2014 the last line of its stdout, in m0serve's report shape (`m0_http.doctor.Report`: `ok`, `exit`, grouped facts, `checks` each carrying its `fix`) \u2014 and exits with the code serving would exit with for the same arguments, having bound nothing. The report gives the resolved configuration, where each value came from (`flag`, `env` or `default`), the topology it adds up to and what the application declares (`max_workers`, `max_threads`, `page_slots`), and never the API key. The code agrees by construction and is gated anyway: `host_checks` is the ONE description of what the host refuses, in the order it refuses, the server exiting 78 on its first failure and the doctor listing them all and exiting on the same one \u2014 so a count that cannot be served is one refusal whether a flag or a variable brought it, never a usage error for one and a 78 for the other. An application's own checks run in its `main` before `serve` and so fire identically under the doctor. NOT covered, because finding out means doing it: the bind, and the application's `make`",
      "status": "verified",
      "evidence": "`Smoke test the Mojo host's flags and doctor` (every PR) \u2014 twenty configurations of `apps/host_check` are each run both ways and the two exit codes must agree with each other and the table: five served (0, really served and drained), seven refused (78: both modes by flag and by one flag and one variable, zero loops, zero workers, more workers and more loops than the application declares, exec'd workers), one tripping two refusals at once where both must name the first, six usage errors (2, no report), and the application's own refusal (78 both ways, the doctor never reached); every report's `exit` and `ok` match its process, a refusal's first failed check is the named one and its `detail` and `fix` are the line the server printed, no doctor run logs a listener, and doctoring a port a live server holds exits 0. The unit halves are `test_host_flags.mojo:test_the_doctor_leaves_with_the_servers_code`, `test_the_checks_are_whole_and_in_the_order_serve_refuses` and `test_a_count_that_cannot_be_served_is_a_refusal_not_a_usage_error`. `sabotage-host` (pre-release) has the doctor ignore what the application declares, always exit 0, and start the server anyway; has the server refuse by its last failed check; and makes zero loops a usage error for the flag alone"
    },
    {
      "id": "E32",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "The Mojo host refuses forked workers when MAX's parallel runtime is linked: `M0_WORKERS` above 1 in a binary that carries `libAsyncRTMojoBindings` \u2014 what `max.algorithm.parallelize` needs, and whose worker threads a `fork()` does not copy, so a `parallelize` in a forked worker never returns \u2014 exits 78 before the bind, naming `--threads (M0_THREADS)` as the fix, and `--doctor` reports the same check (`workers-vs-parallel-runtime`, last in `host_checks`); the fact is read off the loaded images (`RTLD_NOLOAD` on Linux, dyld's image list on macOS), never by calling MAX, so a binary without it answers the check as passed (a `mojo run` has no binary of its own: it runs in the compiler's process, which maps the runtime once `max-core` is installed, so there every program reads as linked \u2014 ROADMAP Known issues); and the same binary serves `parallelize` from one loop, from N loops on N threads and from a handler-pool thread",
      "status": "verified",
      "evidence": "`Smoke test the Mojo host with MAX's parallel runtime` (every PR) \u2014 `apps/host_parallel`, built against `max-core` synced by that step alone (the `max` dependency group, synced by that step and used by E33's alone), refused by the doctor and by the server at two workers, served at two loops, at a pool of two and alone, each `/par` answered and eight at once, every wait bounded so a refusal that stopped refusing fails on a served prefork rather than hanging CI (`sabotage-host --only parallel`); measured before the refusal: the request under two forked workers never answered, the worker's loop stayed wedged and SIGTERM had not ended the process after 12 s. The verdict with the fact supplied by hand is `test_host_flags.mojo:test_prefork_is_refused_when_the_parallel_runtime_is_linked`; and, since 2026-09-25, the release recipe run on a copy of the same binary \u2014 `relocate.py`, then `bundle_artifact.py`, the two scripts the `m0` wheel ships unedited \u2014 with `libAsyncRTMojoBindings` required beside it and the bundle answering `--doctor` and serving `/par` from its own directory with the build venv unreachable (N41's other half, measured where MAX is installed)"
    },
    {
      "id": "E33",
      "section": "E",
      "category": "E. Process and worker model",
      "capability": "m0serve refuses a forked worker when MAX's parallel runtime is linked: a Mojo mount that imports `max.algorithm.parallelize` puts `libAsyncRTMojoBindings` in the image, and `--workers N` above 1 or `--reload` (which supervises even one worker, forked) then exits 2 before the bind, naming `--spawn-workers`, whose worker execs the binary and starts the runtime fresh; `--doctor` reports the same check (`workers-vs-parallel-runtime`) and `topology.parallel_runtime`; the fact is `m0_http.parallel_runtime_linked`, the one function the Mojo host reads (E32), so the shipped `bin/m0serve` passes the check at two workers where its load commands lack the runtime and is refused where they name it \u2014 on macOS a build made beside an installed `max-core` links `libAsyncRTMojoBindings` whether or not the source imports MAX (ROADMAP Known issues)",
      "status": "verified",
      "evidence": "`Smoke test m0serve with MAX's parallel runtime` (every PR) \u2014 `apps/serve_parallel/mount` built in place of the demo mount (N14) over `host_parallel.compute`, E32's job, under `max-core`: refused by the doctor and by the server at two workers and by the doctor under `--reload`, passed under `--workers 2 --spawn-workers` and `--reload --spawn-workers`, the shipped binary's verdict held to its own load commands (`otool -L`, `readelf -d`: passed at two workers on Linux, refused on macOS beside the installed MAX, the doctor's `parallel_runtime` agreeing either way; measured on CI 2026-09-25: the demo-mount `bin/m0serve` bundled three runtime files in the MAX-free `apple-silicon` job and four, the runtime among them, after `uv sync --group max`), then served \u2014 two exec'd workers each answer `/par/par` (by `x-pid`, sixteen at once), `/par/ser` agrees on the digest, SIGTERM drains the supervisor to 0, and one process answers from a pool thread \u2014 every wait bounded so a refusal that stopped refusing fails on the served prefork rather than hanging CI. Measured with the server's refusal removed (2026-09-25, Linux, four cores): the forked worker answered `/par/ser` in 23 ms and `/par/par` never, twice, one pool thread per worker gone until the drain's 5 s join abandoned it; the probe failed at its served-prefork phase. The verdict with the fact supplied by hand is `test_cli.mojo:test_parallel_runtime_forked_truth_table`, in a binary that links nothing"
    },
    {
      "id": "F1",
      "section": "F",
      "category": "F. Observability",
      "capability": "Access log records cannot be forged by a value (newline, quote, backslash escaped)",
      "status": "verified",
      "evidence": "`test_log.mojo:test_a_newline_cannot_forge_a_second_log_line` (every PR)"
    },
    {
      "id": "F2",
      "section": "F",
      "category": "F. Observability",
      "capability": "`--access-log` emits one JSON record per response, and nothing without it",
      "status": "verified",
      "evidence": "`Smoke test the serve CLI` (every PR) \u2014 `--access-log`"
    },
    {
      "id": "F3",
      "section": "F",
      "category": "F. Observability",
      "capability": "`--metrics` turns `/__metrics` from the application's 404 into a 200",
      "status": "verified",
      "evidence": "`Smoke test the serve CLI` (every PR) \u2014 `--metrics`"
    },
    {
      "id": "F4",
      "section": "F",
      "category": "F. Observability",
      "capability": "Prometheus exposition 0.0.4: 8 counter and gauge families, each with HELP, TYPE and a sample",
      "status": "verified",
      "evidence": "`Smoke test the serve CLI` (every PR) \u2014 `--metrics`"
    },
    {
      "id": "F5",
      "section": "F",
      "category": "F. Observability",
      "capability": "Latency histograms on `/__metrics`",
      "status": "verified",
      "evidence": "`Smoke test the serve CLI` (every PR) \u2014 six log-spaced `le` bounds (100\u00b5s\u20131s, +Inf), integer-only and O(1) on the loop thread; `scripts/histogram_check.py` (selftested in the same phase) asserts the documented bounds, non-decreasing cumulative counts, `le=\"+Inf\"` equal to `_count`, and a `_count` covering the phase's own requests; boundary math pinned by `test_metrics.mojo`"
    },
    {
      "id": "F12",
      "section": "F",
      "category": "F. Observability",
      "capability": "Coverage declared by the gate rather than cited by this page",
      "status": "verified",
      "evidence": "`Check machine-sourced doc facts` (every PR) \u2014 every pull-request-gated row declares its coverage in the gate itself (a `covers:` docstring line in the cited test, or a recorder coverage call in what the cited step runs), and the checker requires the declaration to agree with the citation, which is what makes the audit's mis-citation class structurally impossible. Weekly and pre-release rows keep declared-static citations, their runs being absent from PR CI. Four sabotages in `poe sabotage-spec` revert the rules"
    },
    {
      "id": "F6",
      "section": "F",
      "category": "F. Observability",
      "capability": "`--health-path` answers 200",
      "status": "verified",
      "evidence": "`Smoke test the Django realtime example` (every PR) \u2014 `--health-path`"
    },
    {
      "id": "F7",
      "section": "F",
      "category": "F. Observability",
      "capability": "Health registry with readiness aggregation",
      "status": "verified",
      "evidence": "`test_health.mojo:test_health_register_unhealthy` (every PR)"
    },
    {
      "id": "F8",
      "section": "F",
      "category": "F. Observability",
      "capability": "Configuration report that exits as the server would",
      "status": "verified",
      "evidence": "`Smoke test --doctor against the server's own exit codes` (every PR) \u2014 `--doctor`"
    },
    {
      "id": "F9",
      "section": "F",
      "category": "F. Observability",
      "capability": "OpenTelemetry tracing",
      "status": "out of scope",
      "evidence": "no tracing context crosses the Mojo/Python seam today; a wrapper in the application is the supported route"
    },
    {
      "id": "F10",
      "section": "F",
      "category": "F. Observability",
      "capability": "CI measurements recorded, rendered per run and kept as an artifact",
      "status": "verified",
      "evidence": "`Check machine-sourced doc facts` (every PR)"
    },
    {
      "id": "F11",
      "section": "F",
      "category": "F. Observability",
      "capability": "The measurement recorder itself",
      "status": "verified",
      "evidence": "`Self-test the measurement recorder` (every PR)"
    },
    {
      "id": "F14",
      "section": "F",
      "category": "F. Observability",
      "capability": "The site's deploy image: the wheel in `python:3.12-slim` with the built site, served from a container as it deploys",
      "status": "verified",
      "evidence": "`Smoke test the documentation site's deploy image` (every PR) \u2014 `deploy/site/Dockerfile` built from the tree's own wheel; through a published port, `llms.txt` and the sitemap answer at the root, a page answers with the Markdown twin it advertises, the slash redirect and the HTML 404 come from the application, m0serve is PID 1 by `/proc/1/cmdline`, and `docker stop` is the drain's exit 0 inside its grace"
    },
    {
      "id": "F15",
      "section": "F",
      "category": "F. Observability",
      "capability": "Every RFC the tree cites is a current document",
      "status": "verified",
      "evidence": "`Check machine-sourced doc facts` (every PR) \u2014 `scripts/check_citations.py` looks every `RFC nnnn` in a tracked text file up in `scripts/rfc_status.json`, a committed snapshot of the RFC Editor's per-document JSON; a citation with no entry fails, and a citation of an obsoleted RFC fails unless the same paragraph cites a successor. Section numbers are not checked. Selftested in `check-docs`; each rule reverted against the tree by the sabotage step in docs.yml"
    },
    {
      "id": "F16",
      "section": "F",
      "category": "F. Observability",
      "capability": "The RFC snapshot is compared with the RFC Editor monthly",
      "status": "verified",
      "evidence": "`Compare the snapshot with the RFC Editor` (monthly) \u2014 `citations.yml` re-fetches every snapshot entry and fails on a changed `obsoleted_by` or status, filing an issue; a transport failure fails too, because the job exists only to ask. F15 checks that this workflow still exists, has a cron and passes the live flag"
    },
    {
      "id": "F13",
      "section": "F",
      "category": "F. Observability",
      "capability": "The documentation site: the tree's own pages rendered and served by the server, `llms.txt` at the root, a Markdown twin beside every page",
      "status": "verified",
      "evidence": "`Smoke test the documentation site` (every PR) \u2014 built by `scripts/docsite.py`, served through `--static` with `apps/site` behind it; every sitemap URL answers as HTML with the twin it advertises, the root text files name the site's pages by absolute URL, the sitemap goes out as XML, and the slash redirect and HTML 404 come from the application"
    },
    {
      "id": "G1",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "An injected status reason phrase is emptied, not transmitted",
      "status": "verified",
      "evidence": "`test_response.mojo:test_status_reason_with_crlf_is_emptied_not_transmitted` (every PR)"
    },
    {
      "id": "G2",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "A response header carrying CR, LF or NUL is dropped, and a clean one beside it is not",
      "status": "verified",
      "evidence": "`Conformance test the WSGI bridge` (every PR)"
    },
    {
      "id": "G3",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "An application's `Set-Cookie` reaches the wire verbatim",
      "status": "verified",
      "evidence": "`test_response_cookies.mojo:test_raw_line_reaches_the_wire_verbatim` (every PR)"
    },
    {
      "id": "G4",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "`Proxy` request header never becomes `HTTP_PROXY` (httpoxy)",
      "status": "verified",
      "evidence": "`test_environ.mojo:test_proxy_header_is_excluded_from_the_environ` (every PR)"
    },
    {
      "id": "G5",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "Path traversal rejected (`../`)",
      "status": "verified",
      "evidence": "`test_static.mojo:test_dotdot_is_rejected` (every PR)"
    },
    {
      "id": "G6",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "Percent-encoded traversal rejected (`%2e%2e`)",
      "status": "verified",
      "evidence": "`test_static.mojo:test_encoded_dotdot_is_rejected` (every PR)"
    },
    {
      "id": "G7",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "Reserved `x01` channel namespace refused by `publish_to_channels`",
      "status": "verified",
      "evidence": "`test_broadcast.mojo:test_publish_rejects_reserved_channel` (every PR)"
    },
    {
      "id": "G8",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "...and by `BroadcastBus.publish`",
      "status": "verified",
      "evidence": "`test_broadcast.mojo:test_bus_publish_method_rejects_reserved_channel` (every PR)"
    },
    {
      "id": "G9",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "API key authentication, length-checked so a repeated key fails",
      "status": "verified",
      "evidence": "`test_auth.mojo:test_a_rotation_of_the_key_is_rejected` (every PR)"
    },
    {
      "id": "G10",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "CORS, configurable",
      "status": "verified",
      "evidence": "`Smoke test the notes API` (every PR)"
    },
    {
      "id": "G11",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "`X-Forwarded-*` / `Forwarded` parsing with a trusted-proxy allowlist",
      "status": "out of scope",
      "evidence": "the server never consults them \u2014 `REMOTE_ADDR` is the socket peer and `wsgi.url_scheme` is configuration, so there is nothing to spoof"
    },
    {
      "id": "G12",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "PROXY protocol v1/v2",
      "status": "out of scope",
      "evidence": "same reason: the peer address is taken from the socket"
    },
    {
      "id": "G13",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "Parser fuzzing in CI",
      "status": "verified",
      "evidence": "`Fuzz the request decoder` (every PR) \u2014 20k mutations of a seed corpus against `parse_request_headers` and the chunked decoder, replayable from the seed it prints; asserts determinism, that an INVALID request cannot become valid by appending, that a parsed one is unchanged by bytes after it, and that the decoder's counts index its buffer. `poe fuzz-request-long` is the release sweep (8 seeds x 250k). The run refuses to pass on thin coverage, and `Sabotage the decoder invariants the fuzzer checks` breaks each invariant so \"no findings\" cannot mean \"checks nothing\""
    },
    {
      "id": "G14",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "A request carrying bytes that are not UTF-8 \u2014 in the target, a static-mount path, a cookie, `Accept`, `If-None-Match`, `Range` or a JSON body a number is read from \u2014 is answered, never trapped, and so is one whose bytes come back out in a RENDERED FRAGMENT an application broadcasts: every slice of such a string on the serving path is a byte-span slice, which has no codepoint-boundary assert",
      "status": "verified",
      "evidence": "`test_unquote.mojo:test_invalid_utf8_beside_an_escape_does_not_trap` (every PR) \u2014 `unquote` sliced its input as a `String` and one `GET /?x=<0x80>%41` killed the process on the loop thread, every app alike; the cookie jar (parsed for every request), the static mount, the negotiator and the ETag matcher had the same shape, and so did the SSE line splitter and frame decoder, which bytes from outside the request reach too \u2014 a `--pg-listen` payload from a database that converts nothing, trapping worker 0's listener thread \u2014 each pinned by a test in its own file that declares this row, and a trap ends the test process, so the gate is those files passing at all. `m0-datastar`'s `split_data_lines` is a deliberate copy of that splitter, kept dependency-free rather than imported, and needed the same fix one release later \u2014 reached not by a request directly but by a RENDERED FRAGMENT, which is where an application puts request data: a todo whose text carries a newline then a lone 0x80 byte, HTML escaping having left both alone, trapped the LOOP thread and killed the whole server on one unauthenticated POST. `Smoke test the hello server` and `Smoke test the notes API` send the same bytes over a socket and require an answer with the server still up, and `Smoke test the Datastar todo demo` posts such a todo and requires the server to answer it, stay up and keep rendering. `m0-core`'s `parse_json_number` cut the number out of a body with a `String` slice, so `{\"x\":1<0x80>}` trapped; no app called it until `apps/blobs`' drop view, and `Smoke test the blobs demo` posts that body and requires an answer and a healthy server"
    },
    {
      "id": "G15",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "SHA-256 and HMAC-SHA256 in `m0-core`, with a constant-time tag compare: FIPS 180-4's examples and the empty message, every split of one message across `update` calls, and RFC 4231's seven cases including the truncated one and the two whose keys exceed the block size; `HmacSha256` absorbs its padded key once and finishes each message from a copy, and `verify` refuses a truncated tag",
      "status": "verified",
      "evidence": "`test_hmac.mojo:test_rfc_4231_case_7` (every PR) \u2014 the hash's own vectors are `test_sha256.mojo:test_fips_180_4_examples`; the first cryptographic primitive in the tree, and the one a signed grant and a signed session cookie (N13) rest on"
    },
    {
      "id": "G16",
      "section": "G",
      "category": "G. Security hardening",
      "capability": "Sockets the server creates are close-on-exec: a process the application starts with exec inherits no client connection and no internal channel",
      "status": "verified",
      "evidence": "`Smoke test what a child process the application starts inherits` (every PR) \u2014 a view starts a child with close_fds=False in six shapes (the pool, the loop, forked and spawned workers, `--realtime`, the ASGI executor) and requires it to hold no descriptor above 2: the server started with none there, so each would be its own. Before the fix every shape failed, the child holding 8 to 46 of them: connections, the listener, every channel, the shutdown pipe, the shared page. `test_cloexec.mojo` holds each creation helper, including those no shape reaches (the per-loop listener dups and fan-out pipes of `--threads`), and `test_respawn.mojo` that the spawn's own exec keeps exactly what the new image adopts. On macOS each creation call (`accept`, `socket`, `socketpair`, `pipe`, `recvmsg`) is followed by a second that marks the descriptor, so a fork and exec on another thread in the instant between can still inherit that one, as under CPython there; Linux creates each close-on-exec atomically. A child that forks WITHOUT exec (`os.fork`, multiprocessing's fork start method) inherits everything, which no descriptor flag can prevent"
    },
    {
      "id": "H1",
      "section": "H",
      "category": "H. TLS",
      "capability": "TLS 1.2 / 1.3 termination",
      "status": "out of scope",
      "evidence": "terminate at a proxy \u2014 gunicorn's answer, and the same one applies here"
    },
    {
      "id": "H2",
      "section": "H",
      "category": "H. TLS",
      "capability": "ALPN, SNI, mTLS, OCSP stapling, certificate hot-reload",
      "status": "out of scope",
      "evidence": "all follow from not terminating TLS"
    },
    {
      "id": "I1",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "WebSocket handshake (RFC 6455), `Sec-WebSocket-Accept`",
      "status": "verified",
      "evidence": "`Smoke test the WebSocket echo demo` (every PR)"
    },
    {
      "id": "I2",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "Fragmented messages reassembled",
      "status": "verified",
      "evidence": "`test_websocket.mojo:test_fragmented_message_assembles` (every PR)"
    },
    {
      "id": "I3",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "Ping/pong, and a server heartbeat on a cadence",
      "status": "verified",
      "evidence": "`Smoke test the WebSocket echo demo` (every PR)"
    },
    {
      "id": "I4",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "Close frame echoed with its code, connection marked for close",
      "status": "verified",
      "evidence": "`test_websocket.mojo:test_close_is_echoed_with_code_then_closes` (every PR)"
    },
    {
      "id": "I5",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "Invalid UTF-8 in a text frame closes 1007",
      "status": "verified",
      "evidence": "`test_websocket.mojo:test_invalid_utf8_text_closes_1007` (every PR)"
    },
    {
      "id": "I6",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "A fragmented control frame is a protocol error",
      "status": "verified",
      "evidence": "`test_websocket.mojo:test_fragmented_control_frame_is_protocol_error` (every PR)"
    },
    {
      "id": "I7",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "Wrong `Sec-WebSocket-Version` answers 426 advertising 13",
      "status": "verified",
      "evidence": "`test_websocket.mojo:test_wrong_version_is_426_advertising_13` (every PR)"
    },
    {
      "id": "I8",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "Cross-worker WebSocket fan-out over the broadcast bus",
      "status": "verified",
      "evidence": "`Smoke test the WebSocket chat demo` (every PR)"
    },
    {
      "id": "I9",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "Server-Sent Events, with heartbeats and disconnect cleanup",
      "status": "verified",
      "evidence": "`Smoke test the Datastar counter` (every PR)"
    },
    {
      "id": "I10",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "`Last-Event-ID` replay from a bounded journal",
      "status": "verified",
      "evidence": "`Smoke test the Datastar todo demo` (every PR)"
    },
    {
      "id": "I11",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "A synchronous view gating a held SSE connection, with cross-worker publish",
      "status": "verified",
      "evidence": "`Smoke test the Django realtime example` (every PR) \u2014 `--realtime`"
    },
    {
      "id": "I12",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "A synchronous view gating a held WebSocket it never speaks",
      "status": "verified",
      "evidence": "`Smoke test the Django realtime example over WebSockets` (every PR)"
    },
    {
      "id": "I18",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "N holds taken AT ONCE from a pool, and a publish reaching all of them",
      "status": "verified",
      "evidence": "`Concurrent --realtime holds from a pool` (every PR) \u2014 the other realtime rows subscribe sequentially, so this is the only one where more than one hold is in flight; the probe's own self-test gates it, because the result it reports is a negative"
    },
    {
      "id": "I19",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "Inbound WebSocket data larger than one socket read",
      "status": "verified",
      "evidence": "`Inbound WebSocket messages survive a stalled client` (every PR) \u2014 the WebSocket read path took one `recv` per event with no re-arm, A13's defect in the one path nothing had sent a large inbound burst to; invisible on kqueue's level trigger, and on epoll only once the client STOPS sending"
    },
    {
      "id": "I13",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "Autobahn|Testsuite conformance run, wired to a cadence",
      "status": "verified",
      "evidence": "`autobahn` (pre-release) \u2014 sections driven separately against the runner's pure-echo ASGI app, image version-pinned so the per-section case counts are asserted exactly; compared both directions against the pinned baseline (240/247, every failure I17's cap): a new failure is red, and an I17 case unexpectedly passing is red too. The comparator's selftest runs first. What the suite cannot see stays L15's territory \u2014 its client always closes first (ROADMAP: A conformance-suite tier)"
    },
    {
      "id": "I14",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "`permessage-deflate`",
      "status": "out of scope",
      "evidence": "follows from having no response compression"
    },
    {
      "id": "I15",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "WebSocket over HTTP/2 (RFC 8441)",
      "status": "out of scope",
      "evidence": "follows from having no HTTP/2"
    },
    {
      "id": "I16",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "A Close frame's code is VALIDATED, not just echoed",
      "status": "verified",
      "evidence": "`test_websocket.mojo:test_reserved_close_codes_are_refused_1002` (every PR) \u2014 with `test_legal_close_codes_are_still_echoed` as the other half, so a refusal that refuses everything cannot pass"
    },
    {
      "id": "I17",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "A message at or above the outbox cap ends the connection",
      "status": "verified",
      "evidence": "`Smoke test the outbox cap ending a connection` (every PR) \u2014 the marker sent after the oversized message must never arrive, which is what separates ending the connection from dropping a frame the peer cannot know it missed; the under-cap half stops a server that ended every large-message connection from passing. Deliberate, and what Autobahn scores as 7 failures plus all of its performance section. `poe sabotage-outbox-cap` (pre-release) reverts each of the four rules"
    },
    {
      "id": "I20",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "A synchronous Flask view gating a held SSE stream and a WebSocket, with cross-worker publish",
      "status": "verified",
      "evidence": "`Smoke test the Flask realtime views` (every PR) \u2014 the headline names Flask, and K10 only proves plain WSGI. The file under test is extracted from docs/QUICKSTART_NEXT.md's own fenced block, so it is the one a reader types; the Django rows' RFC 6455 probe drives it unchanged (its gate phase off: the quickstart's views take no token), one stream and one socket pinned per worker under `--workers 2`, so a message sent on one worker's socket reaches a Flask view and comes back on the other worker's. The quickstart runs the same file from the wheel with curl alone (M10)"
    },
    {
      "id": "I21",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "A grant-verified hold mount: `--mount PREFIX=hold` holds an SSE stream on a Mojo pool thread against a grant the application signed into the stream URL (`m0serve.grant`: version, key id, expiry, channel, session binding, HMAC-SHA256 tag) \u2014 the signature checked in constant time, expiry against the host clock, the session cookie the browser sends against the binding, the previous key accepted while `M0_GRANT_KEY_PREV` lists it, a bound grant refused without its cookie and an unbound one admitted, every refusal a 401 naming `expired` or `invalid` and its reason; a hold mount refuses to start without `--realtime` or `M0_GRANT_KEY`, and `--doctor` agrees",
      "status": "verified",
      "evidence": "`Smoke test the grant-verified hold mount` (every PR) \u2014 the verifier's unit form is `test_grant.mojo:test_a_grant_the_issuer_signed_admits_its_session`, against grants the same issuer signed with a fixed key and clock; the design is [grant-verified-holds](notes/grant-verified-holds.md)"
    },
    {
      "id": "I22",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "A Postgres `NOTIFY` reaches a held stream: `--pg-listen URL` holds one `LISTEN` on worker 0 and publishes what arrives onto the broadcast bus, so a writer that cannot reach the datagram bus at all \u2014 a trigger, a cron job, a management command, `psql` \u2014 reaches every subscriber with an id, an event type and its data; a notification naming another channel reaches nobody, a malformed payload \u2014 including an `event` or `data` present as anything but a JSON string, such as the object `row_to_json(NEW)` makes \u2014 is refused and counted rather than delivered somewhere or delivered empty, exactly one listener runs under N workers so an event is delivered once rather than N times, a host with no libpq is a refusal rather than a server that runs with no listener, and without the flag neither does one naming the held channel",
      "status": "verified",
      "evidence": "`Smoke test a Postgres NOTIFY reaching a held stream` (every PR) \u2014 three arms, because the positive one alone passes against a server that published everything to everyone: the held channel arrives, another channel does not, a payload with no channel, no JSON at all, or a `data` that is a JSON object (which a string reader answers as empty, and which reached subscribers as an empty `data:` line counted as delivered until it was refused \u2014 measured on the gate before the fix, and pinned per shape without a server by `test_pg_envelope.mojo:test_data_that_is_not_a_string_is_refused` in `poe test-all`) is counted as refused while the listener survives to deliver the next good one (asserted as a COUNT of events on a guarded channel, because a by-name check passes against a refusal that delivers an empty event \u2014 measured), the same publish against a server started without the flag reaches nobody, and a two-worker server starts exactly one listener and delivers one copy \u2014 in each platform's own worker shape, forked on Linux because that is what a deploy runs, spawned on macOS because a forked child there is refused. `--pg-listen` without `--realtime` exits 2 naming what is missing, since the flag creates the bus there would otherwise be nothing to publish into; on macOS it also exits 2 wherever a worker is FORKED \u2014 `--workers N`, and `--reload`, which supervises even one, and which the refusal first missed because it tested the worker count, so the gate runs both shapes through the server and the doctor on macOS and asserts the doctor refuses neither on Linux \u2014 because libpq's connect reaches Kerberos through GSSAPI and Objective-C aborts a forked child \u2014 measured as the worker killed by signal 9 and respawned until the supervisor gave up, which is the churn CLAUDE.md records for `_scproxy`. Both refusals run before the bind and the fork, where the supervisor can report them; placed after it they ran in every child and none in the parent, so a usage error read as a crash loop. A host with no libpq exits 78 (sysexits' `EX_CONFIG`) naming every path tried, which the WHEEL's own binary is held to by `Build and smoke test the installable wheel` \u2014 the artifact whose users have neither a toolchain nor a libpq, and the place an absent library would otherwise degrade into a listener that silently hears nothing. In the Linux job with a service container, GitHub's service containers requiring a Linux runner. The gate exercises the prefork path; `--threads` starts the same listener once per process and is NOT gated, because CI pins a GIL-enabled interpreter and the mode refuses to start on one \u2014 verified by hand on free-threaded CPython 3.14.7 on 2026-09-12, a `NOTIFY` reaching a stream held across two loops, and confirmed to have been a silent no-op before it was wired into that path"
    },
    {
      "id": "I23",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "A child process a view starts can publish onto the bus: `m0pub.child_fds()` passed as `pass_fds` hands it the bus and the event-id page by descriptor, and its frames are numbered from the shared counter; a child that inherited the environment without the page, or with an unrelated file on the page's old descriptor number, publishes unnumbered frames and never takes an id from memory that is not the page",
      "status": "verified",
      "evidence": "`Smoke test publishing from a child process` (every PR) \u2014 `bareapp.childpub` under `--realtime` with one worker, `--workers 2` and `--spawn-workers`: a child given `child_fds()` must take an id strictly between two the parent takes around it and carry it on the SSE frame; children that inherited everything, had `M0_SHARED_ID_ADDR` removed, or found a file of the page's size on `M0_SHARED_ID_FD` must exit 0 and publish unnumbered, the last with that file unwritten; a child handed no bus must write nothing into whatever sits on a `M0_BUS_WRITE_FDS` number -- an unrelated file, left unwritten, or its own datagram socket, which receives nothing and still blocks -- and malformed numbers degrade to nothing written. m0pub keeps a number only when `os.fstat` finds the device and inode `M0_BUS_WRITE_IDS` records for it: the bus is close-on-exec since G16, and before this m0pub wrote into the file, and a check by kind wrote into the socket and made it non-blocking. Before the fix the inheriting child died with SIGSEGV (one worker) or published an id taken from its own memory (the other two shapes), measured on macOS; #322 reported the SIGSEGV on Linux. Reverting the magic check at the address, the kernel readability check, the magic check on the descriptor, the page in `child_fds()`, the server's magic word or the file-backed page each fails it"
    },
    {
      "id": "I24",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "A stream of states: `DatastarStream(send_latest=True)` keeps the newest frame per url by event id \u2014 from a local broadcast or from the bus \u2014 and sends it to every subscriber at `open`, first visit and reconnect alike, then the live feed; it never replays, and it ignores the `Last-Event-ID` a client presents, because a producer's ids restart with its process and comparing would park a tab from the previous process until the new ids passed its old one; an older frame arriving late neither replaces the newest nor reaches a subscriber that has it",
      "status": "verified",
      "evidence": "`test_stream.mojo:test_state_stream_sends_the_newest_frame_at_open` (every PR) \u2014 its control is the default stream fed the same frames, which sends nothing at open; `test_state_stream_ignores_the_presented_id` presents an id behind, level, far ahead and malformed; `test_state_stream_keeps_the_newest_by_id` delivers frame 2 after frame 3. Sabotaged four ways against those tests (no send at open, older replaces newer, the presented id compared, bus frames not kept), each caught. On the wire, `Smoke test the blobs demo` waits for its producer to pause so the newest id is fixed, then requires a new stream's first frame to be exactly that id and nothing older"
    },
    {
      "id": "I25",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "A bus publish reports what it delivered: `publish_to_channels` (and `BroadcastBus.publish`) returns how many channels took the datagram \u2014 0 for a frame over `BUS_MAX_FRAME`, a reserved channel or a name longer than the wire's two-byte length, and fewer than addressed when a channel is full \u2014 so a producer whose frame grows with its state can count a refusal that is otherwise indistinguishable from a producer that stopped",
      "status": "verified",
      "evidence": "`test_broadcast.mojo:test_publish_reports_what_it_delivered` (every PR) \u2014 the partial case stuffs one of three channels until it refuses and requires the next frame to count two; counting before the send instead of after it is caught there. `Smoke test the blobs demo` requires its producer's refusal count to stay 0 at a full stage"
    },
    {
      "id": "I26",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "An inbound WebSocket message larger than the channel's datagram is refused with a Close carrying 1009, logged once, never parked",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR) \u2014 a 70,000-byte message to an executor-held socket must be answered with a 1009 Close within 5 s and the application must hear 1009, while a 65,536-byte one is still delivered whole; `Smoke test the Django realtime example over WebSockets` sends the same to a pool-held socket and requires the connection to END on the client's reply, with no second Close (Chromium reports a second one as a failed connection, 1006), and within the linger when the client never replies; a message sent right behind the refused one reaches no one (RFC 6455 \u00a77.1.7). Before, the message was parked and retried for ever: no reply, no close, no log"
    },
    {
      "id": "I27",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "Datastar frames are the SDK's own: every conformance case the Datastar SDK ships at the version `m0-datastar` pins \u2014 its get-cases and its post-case, vendored byte for byte with each file hashed \u2014 is produced by the package's builders and judged by the SDK's own `compare-sse.sh`, which lets datalines of different keys arrive in any order and nothing else: multi-line `signals` go out one dataline per line, `remove` mode carries no `elements` line, and `execute_script` writes its `attributes`",
      "status": "verified",
      "evidence": "`Run unit tests` (every PR) \u2014 `poe check-datastar-sdk`, inside `test-all`, refuses fixtures from any tag but the one `consts.VERSION` pins and any vendored file whose hash has changed, and fails by name on a fixture field it does not map; `poe sabotage-datastar-sdk`, inside `test-all` too, reverts each of the three rules in a copy of the package and each is caught by the case it breaks. Before the gate the package passed 14 of the 19 get-cases"
    },
    {
      "id": "I28",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "`read_signals` reads the signals where the client sends them: the `datastar` query parameter on GET and DELETE \u2014 the two methods Datastar's bundle sends without a body, and the SDK's `ReadSignals` table \u2014 and the body on every other method; each SDK case's input comes back byte for byte as a GET, a DELETE and a POST",
      "status": "verified",
      "evidence": "`test_stream.mojo:test_read_signals_from_delete_query` (every PR) \u2014 a DELETE's query is read and its body ignored, and PUT, PATCH and QUERY read the body; `poe check-datastar-sdk` replays every case's input through all three methods, and its sabotage reverts DELETE to the body and loses 19 of 20. A DELETE's signals came back as `{}` before"
    },
    {
      "id": "I29",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "A Datastar frame cannot be split by what it carries: a selector, mode, namespace, view-transition selector or event id holding CR or LF is refused with an error rather than written \u2014 two breaks would end the event and open one of the sender's choosing \u2014 and `redirect` writes its location as a JavaScript string literal with the quote, the backslash, `<`, every control byte and U+2028/U+2029 escaped, so it can end neither the literal nor its `<script>`",
      "status": "verified",
      "evidence": "`test_frame_injection.mojo:test_a_line_break_in_a_single_line_field_is_refused` (every PR) \u2014 every field through every builder, the event-injection shape among them, with a control that a break-free selector still goes out; `test_redirect_location_cannot_leave_its_literal` requires exactly one `</script>` in a frame whose location carries two. `poe sabotage-datastar-sdk` reverts the refusal and three of the escapes, each caught"
    },
    {
      "id": "I30",
      "section": "I",
      "category": "I. WebSocket and Server-Sent Events",
      "capability": "A reconnect the replay journal cannot fully catch up is told so, never served in part: when the client's `Last-Event-ID` is older than a frame the journal evicted or than the process's own history \u2014 its first restored frame, or what siblings sent before a respawned worker joined \u2014 or is ahead of every id the process knows, or when the frames it missed will not fit the connection's 64 KB outbox, `DatastarStream.open` queues none of them and `caught_up(slot)` answers false; `send_to(slot, frame)` queues one unnumbered frame for that connection alone, and `apps/datastar_todo` sends its current list that way. Live frames a full outbox refuses are counted (`refused()`)",
      "status": "verified",
      "evidence": "`test_stream.mojo:test_a_replay_larger_than_the_outbox_is_not_served_in_part` (every PR) \u2014 thirty 3 KB frames used to arrive as frames 1\u201321 with 22\u201330 refused, leaving the client nine changes behind with a clean log; now nothing is queued, the resync arrives, and the live feed resumes at 31. Beside it, one test per source of a gap and a control that a replay which fits is whole; `poe sabotage-datastar-sdk` reverts the rollback, the eviction and history checks, the ahead check, the late worker's floor and the refusal count, each caught. On the wire, `Smoke test the Datastar todo demo` reconnects thirteen 2 KB adds behind and requires the newest todo and no replayed id; with the rollback reverted it fails there"
    },
    {
      "id": "J1",
      "section": "J",
      "category": "J. Static file serving",
      "capability": "Zero-copy `sendfile`, body never entering the process",
      "status": "verified",
      "evidence": "`Smoke test zero-copy static file serving` (every PR) \u2014 `--static`"
    },
    {
      "id": "J2",
      "section": "J",
      "category": "J. Static file serving",
      "capability": "Byte range served as 206 with `Content-Range`",
      "status": "verified",
      "evidence": "`test_static.mojo:test_range_serves_206_with_content_range` (every PR)"
    },
    {
      "id": "J3",
      "section": "J",
      "category": "J. Static file serving",
      "capability": "Unsatisfiable range answered 416 carrying the total",
      "status": "verified",
      "evidence": "`test_static.mojo:test_range_unsatisfiable_is_416_with_total` (every PR)"
    },
    {
      "id": "J4",
      "section": "J",
      "category": "J. Static file serving",
      "capability": "`If-None-Match` takes precedence over a range",
      "status": "verified",
      "evidence": "`test_static.mojo:test_if_none_match_beats_range` (every PR)"
    },
    {
      "id": "J5",
      "section": "J",
      "category": "J. Static file serving",
      "capability": "`If-Range` with a weak ETag serves the full body",
      "status": "verified",
      "evidence": "`test_static.mojo:test_if_range_with_weak_etags_serves_full` (every PR)"
    },
    {
      "id": "J6",
      "section": "J",
      "category": "J. Static file serving",
      "capability": "ETag and conditional 304",
      "status": "verified",
      "evidence": "`Smoke test the notes API` (every PR)"
    },
    {
      "id": "J7",
      "section": "J",
      "category": "J. Static file serving",
      "capability": "`Cache-Control`, configurable",
      "status": "verified",
      "evidence": "`Smoke test the serve CLI` (every PR) \u2014 `--static-cache-control`"
    },
    {
      "id": "J8",
      "section": "J",
      "category": "J. Static file serving",
      "capability": "Response compression (gzip, brotli, zstd)",
      "status": "out of scope",
      "evidence": "recorded in ROADMAP as deliberate: no dynamic compression; a proxy compresses"
    },
    {
      "id": "J9",
      "section": "J",
      "category": "J. Static file serving",
      "capability": "Precompressed sidecar files (`.br`, `.gz`)",
      "status": "out of scope",
      "evidence": "follows from the row above"
    },
    {
      "id": "K1",
      "section": "K",
      "category": "K. WSGI (PEP 3333)",
      "capability": "`application(environ, start_response)` against a bare callable",
      "status": "verified",
      "evidence": "`Conformance test the WSGI bridge` (every PR)"
    },
    {
      "id": "K2",
      "section": "K",
      "category": "K. WSGI (PEP 3333)",
      "capability": "`wsgiref.validate` pass, with an engagement canary",
      "status": "verified",
      "evidence": "`Conformance test the WSGI bridge` (every PR)"
    },
    {
      "id": "K3",
      "section": "K",
      "category": "K. WSGI (PEP 3333)",
      "capability": "The `write()` callable reaches the client, in production order",
      "status": "verified",
      "evidence": "`Conformance test the WSGI bridge` (every PR)"
    },
    {
      "id": "K4",
      "section": "K",
      "category": "K. WSGI (PEP 3333)",
      "capability": "A second `start_response`, with and without `exc_info`",
      "status": "verified",
      "evidence": "`Conformance test the WSGI bridge` (every PR)"
    },
    {
      "id": "K5",
      "section": "K",
      "category": "K. WSGI (PEP 3333)",
      "capability": "`wsgi.input` read, readline, iteration, and read past EOF",
      "status": "verified",
      "evidence": "`Conformance test the WSGI bridge` (every PR)"
    },
    {
      "id": "K6",
      "section": "K",
      "category": "K. WSGI (PEP 3333)",
      "capability": "`QUERY_STRING` raw while `PATH_INFO` is decoded",
      "status": "verified",
      "evidence": "`Conformance test the WSGI bridge` (every PR)"
    },
    {
      "id": "K7",
      "section": "K",
      "category": "K. WSGI (PEP 3333)",
      "capability": "`close()` called on the response iterable",
      "status": "verified",
      "evidence": "`Conformance test the WSGI bridge` (every PR)"
    },
    {
      "id": "K8",
      "section": "K",
      "category": "K. WSGI (PEP 3333)",
      "capability": "Correct `wsgi.multithread` / `wsgi.multiprocess` for the real topology",
      "status": "verified",
      "evidence": "`Smoke test the Django WSGI example` (every PR)"
    },
    {
      "id": "K9",
      "section": "K",
      "category": "K. WSGI (PEP 3333)",
      "capability": "Unsized iterables streamed from a pool thread, sized bodies buffered",
      "status": "verified",
      "evidence": "`Smoke test streamed WSGI bodies` (every PR)"
    },
    {
      "id": "K10",
      "section": "K",
      "category": "K. WSGI (PEP 3333)",
      "capability": "Framework-neutral: one contract, two frameworks",
      "status": "verified",
      "evidence": "`Run the WSGI framework contract against Flask` (every PR)"
    },
    {
      "id": "K11",
      "section": "K",
      "category": "K. WSGI (PEP 3333)",
      "capability": "The hold headers degrade under another WSGI server: the same view answers a short plain response",
      "status": "verified",
      "evidence": "`Execute the quickstart` (every PR) \u2014 the quickstart's Django file under gunicorn: `/events` answers 200 with the view's body and closes inside curl's deadline (held, it would not), the upgrade request answers 200 rather than 101, and `publish()` reports 0 workers without raising. The README's \"degrades, not breaks\" sentence, executed"
    },
    {
      "id": "K12",
      "section": "K",
      "category": "K. WSGI (PEP 3333)",
      "capability": "The gateway sends the application's response head as sent: no invented `Content-Type`, and the application's `Content-Length` kept on a HEAD",
      "status": "verified",
      "evidence": "`Conformance test the WSGI bridge` (every PR) \u2014 `scripts/head_probe.py` reads the bare app's heads: a 200 and a 303 sent without a type, a bare 204 and one carrying a length and a body, a bare 304 and one naming the GET's length, and a HEAD answered with the GET's length and no body. Each must carry what the application sent plus the server's own framing and nothing else, and each is followed by a request on the same connection, which a leaked body would misparse. `Conformance test the ASGI bridge` runs the same cases through the executor and the buffered bridge. Before the fix every case failed: `application/octet-stream` on each head sent without a type, `content-length: 0` on the HEAD, the 204 and the 304s"
    },
    {
      "id": "K13",
      "section": "K",
      "category": "K. WSGI (PEP 3333)",
      "capability": "A HEAD to what a GET would stream is answered at the body's first item, with no length, and the body closed",
      "status": "verified",
      "evidence": "`Smoke test streamed WSGI bodies` (every PR) \u2014 a HEAD to a never-ending generator on a four-thread pool, then a request on the same connection, then five more HEADs and a fast request. HEAD used to take the buffered path, which joins the whole iterable, so the HEAD never answered and held its pool thread until shutdown; eight such requests are the whole zero-config pool. L27's WSGI twin. On the loop's own handler, which never streams, a HEAD still buffers and carries its measured length, as its GET does. `poe test-shim` pins that the body is pulled to one item and closed"
    },
    {
      "id": "L1",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "Single `app(scope, receive, send)`, protocol detected from the object",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR) \u2014 `--protocol`"
    },
    {
      "id": "L2",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "`http` scope shape, validated against the spec",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR)"
    },
    {
      "id": "L3",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "`websocket` scope: connect, accept, receive, send, close",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR)"
    },
    {
      "id": "L4",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "`lifespan` startup and shutdown, degrading if unsupported",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR)"
    },
    {
      "id": "L5",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "`lifespan.state` shallow-copied into each request scope",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR)"
    },
    {
      "id": "L6",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "Streaming responses stream, credit-gated per stream and in total",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR)"
    },
    {
      "id": "L7",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "Slot ownership across recycled connections, sabotage-proven",
      "status": "verified",
      "evidence": "`Run unit tests` (every PR) \u2014 `poe test-shim` drives the extracted shim through real socketpairs and reverts each rule"
    },
    {
      "id": "L8",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "The event loop running inside asyncio (`M0_INVERTED`)",
      "status": "verified",
      "evidence": "`Smoke test the ASGI executor under the loop inversion` (every PR)"
    },
    {
      "id": "L9",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "Slot ownership under CPU contention, on the streamed AND WebSocket paths, in both loop modes",
      "status": "verified",
      "evidence": "`stress-asgi` (pre-release) \u2014 each round runs `chunked_keepalive.py` then `ws_probe.py`, so the handshake lands on the slot the streamed connection just released; run under CPU hogs on the pump and again under `M0_INVERTED=1`"
    },
    {
      "id": "L10",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "Django's own ASGI handler through the executor",
      "status": "verified",
      "evidence": "`Serve a Django ASGI project through the executor` (every PR)"
    },
    {
      "id": "L11",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "Starlette-family app (FastHTML) through the executor",
      "status": "verified",
      "evidence": "`Serve a FastHTML app through the ASGI bridge` (every PR)"
    },
    {
      "id": "L19",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "The same Starlette seam driven by FastAPI: routing, `StreamingResponse`, and a WebSocket the app closes itself",
      "status": "verified",
      "evidence": "`Serve a FastAPI app through the ASGI bridge` (every PR) \u2014 the row L11 does not cover, because the two frameworks drive Starlette differently and only this one is named on the landing page. The streamed route is checked in the server LOG as well as the body: marking Starlette's anyio child task instead of the slot's owner left one traceback per streamed response with every body intact, so a green body proves nothing here. Request validation and the OpenAPI page are FastAPI's own to test and are deliberately not asserted"
    },
    {
      "id": "L12",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "Cross-worker pub/sub as `scope[\"state\"][\"m0\"]`",
      "status": "verified",
      "evidence": "`ASGI cross-worker fan-out over the BroadcastBus` (every PR)"
    },
    {
      "id": "L13",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "`http.response.pathsend`",
      "status": "out of scope",
      "evidence": "`--static` serves files in Mojo ahead of the application, which is the same saving without the extension"
    },
    {
      "id": "L14",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "`http.response.zerocopysend`, `early_hint`, `trailers`",
      "status": "out of scope",
      "evidence": "no application has asked; the extensions are additive and can be taken later"
    },
    {
      "id": "L15",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "An app-initiated close ends in a FIN, not an RST (RFC 6455 \u00a75.5.1's order)",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR) \u2014 `ws_probe.py` runs 64 concurrent app-initiated closes and requires every one to end in a clean FIN; concurrency is what widens the window, so one close at a time would pass on the broken server. One window no probe can force: an executor descheduled between the Close and the end marker that starts the wait had the peer's reply echoed as a second Close, on CI twice, so the executor sends the two as one datagram and `test_stream_frames.mojo` pins that shape"
    },
    {
      "id": "L16",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "...and the wait for the peer's reply is BOUNDED, so a peer that never answers does not hold its slot",
      "status": "verified",
      "evidence": "`Smoke test the idle connection timeout` (every PR) \u2014 the linger used to re-arm on every loop pass, which held the slot for the life of the process; L15 alone passes on that server"
    },
    {
      "id": "L18",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "ASGI on a free-threaded CPython build is refused with exit 78, naming modular/modular#5726",
      "status": "verified",
      "evidence": "`py-canary` (weekly) \u2014 `smoke-django-realtime` phase 6 on 3.14t: the executor's Python type cannot be built there (the stdlib lays `PyObject` out for the GIL build), so the mixed server exits 78 alone, through `--doctor`, and under `--workers 2` without a respawn; on the pinned GIL interpreter the same phase runs the full mixed server"
    },
    {
      "id": "L17",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "Inbound `websocket.receive` is flow-controlled: a stalled client is throttled, never silently dropped",
      "status": "verified",
      "evidence": "`Inbound WebSocket messages survive a stalled client` (every PR) \u2014 the loop suspends the read rather than discarding what the executor cannot take; a concurrently-reading client loses nothing even on the broken build, so the gate stalls first and only then reads"
    },
    {
      "id": "L20",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "A send is judged by the connection it addresses, never the task making it: one connection's task reaches another's live socket, and a send kept for a client that has gone never reaches the client given its slot",
      "status": "verified",
      "evidence": "`Run unit tests` (every PR) \u2014 `poe test-shim` recycles a slot under a kept WebSocket send and under a kept stream send, with and WITHOUT a disconnect tag (a socket its application closed, which the loop never tagged), sends from a gone socket's own task to the sockets still connected, answers after the application returned, and sabotages each rule; FastAPI's documented chat room checks the same on the wire in L21's gate"
    },
    {
      "id": "L21",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "A WebSocket's disconnect reaches the application through `receive()`, never as a cancellation, so its own cleanup runs; a send to a socket that has gone raises `ClientDisconnected` (an `OSError`)",
      "status": "verified",
      "evidence": "`Serve a FastAPI app through the ASGI bridge` (every PR) \u2014 FastAPI's documented chat room: its `except WebSocketDisconnect:` must tell the room and shrink the connection list, a new client must receive one copy of a broadcast, and a socket the SERVER ends (one message over the outbox cap) must still reach its application; the two-executor case, where that disconnect is routed by lane, is a phase of the mixed-mount smoke"
    },
    {
      "id": "L22",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "A response is answered at its final body; the application's background tasks run after it, and survive the client leaving",
      "status": "verified",
      "evidence": "`Serve a FastHTML app through the ASGI bridge` (every PR) \u2014 a `BackgroundTask` that sleeps 2 s under a response that must arrive in under 1"
    },
    {
      "id": "L23",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "An application error keeps the application's own error response, and its traceback reaches the log",
      "status": "verified",
      "evidence": "`Serve a FastHTML app through the ASGI bridge` (every PR) \u2014 the raising route must answer Starlette's own 500 body, and the log must carry the traceback"
    },
    {
      "id": "L24",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "A WebSocket application that raises is closed with 1011, not 1000 (RFC 6455 \u00a77.4.1)",
      "status": "verified",
      "evidence": "`Serve a FastAPI app through the ASGI bridge` (every PR) \u2014 the chat probe's raising-socket phase"
    },
    {
      "id": "L25",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "A de-chunked request's scope carries `content-length` and not the `chunked` coding the loop removed, and no header the client did not send",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR) \u2014 a chunked POST and a GET, read back from the scope on the executor and on the buffered hatch: the de-chunked one must carry its length and no `transfer-encoding`, and the GET neither a `content-length` nor a `connection` curl never sent. Before the fix every parsed request went through the outgoing constructor, which filled in a length, a `Connection` and a `Host`, so a GET carried `content-length: 0` and `connection: keep-alive` and a chunked request both halves of a contradictory pair an application proxying it would forward. A `gzip, chunked` body keeps its `gzip`, and HTTP/1.0 still closes by default, now read from the protocol (`test_request_as_sent.mojo`). The WSGI environ follows, since it maps the same headers: `Conformance test the WSGI bridge` sends the same two requests to `/environ`"
    },
    {
      "id": "L26",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "An application that schedules asyncio work at import is refused by name, with the fix",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR) \u2014 a module calling `asyncio.create_task` at import must exit 1 naming what it did and the fix (a lifespan startup handler), keep its traceback and print no ready banner, and `--doctor` and a bare MODULE through discovery must say the same. uvicorn refuses the same module without `--reload`, as `RuntimeError: no running event loop`, which is all m0serve used to print. `poe test-shim` holds the other half: any OTHER `RuntimeError` keeps its own words"
    },
    {
      "id": "L27",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "A HEAD answered by a streaming application is its head and nothing else: answered at its first streamed body, the rest dropped",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR) \u2014 a HEAD to a route that streams 10,000 bytes, then a request on the same connection, which must parse. Before the fix the executor streamed the HEAD like a GET, the loop wrote all 10,000 bytes raw after the head, and the next request found them where its response should begin. The application's later sends are dropped, and it is told its response is over: a receive() parked from before the first body (Starlette's, Django's) is woken with `http.disconnect`, which stops a StreamingResponse without cancelling its background work, and an application that parked none is cancelled if it is still running a second later. A 1xx, 204 or 304 streamed the same way is answered the same way. `poe test-shim` holds each of those rules, sabotage-proven. The loop holds the same line for a hold approved on a HEAD and for a native SSE route: `Smoke test the Django realtime example` and `Smoke test the Datastar counter` each send a HEAD to their stream, then a request on the same connection that must be answered, and require a subscriber count of zero after it. Under the buffered escape hatch a HEAD's stream is buffered like its GET's: a finite one is answered with its measured length and no body, an endless one with the watchdog's 500 after 10 s"
    },
    {
      "id": "L28",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "A WebSocket's disconnect tells the application the code its client closed with: the client's own, 1005 for a Close with no code, 1006 only when no Close was parsed",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR) \u2014 `scripts/ws_close_probe.py` closes with 1001, with no code, and with no Close at all, and reads back what the application heard, on the pump and inverted alike. It was 1006 for all three, where uvicorn passes the client's. A Close, or a last message, sent in the same instant as the hang-up reaches the application too: the loop used to close a socket at EOF before reading what it had buffered, and a raw client that closed that way was heard as 1006 and lost its last message. Under `M0_INVERTED=1` the direct disconnect call drains the submit channel first, or it overtook that message"
    },
    {
      "id": "L29",
      "section": "L",
      "category": "L. ASGI 3.0",
      "capability": "Nothing follows this side's Close: a socket lingering for the peer's reply is skipped by the heartbeat, so the reply is met by a FIN and never by a ping (RFC 6455 \u00a71.4, \u00a75.5.1)",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR) \u2014 `ws_probe.py` holds its Close reply for a second, three heartbeat periods inside the two-second linger, and requires silence and then a FIN; on the server before the fix three pings arrive in that second. Found by `stress-asgi` (pre-release): the 300 ms heartbeat landed inside the window between the Close going out and the reply being read, which CPU hogs widen, in 3 rounds of 30, and the close-order phase read `0x89 0x02 \"hb\"` where it expected the FIN -- reported, until the probe learned to name a frame, as the RST it was not"
    },
    {
      "id": "M1",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "Several applications in one process, routed by prefix",
      "status": "verified",
      "evidence": "`Serve two mounted applications from one process` (every PR) \u2014 `--mount`"
    },
    {
      "id": "M2",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "Flags over environment over defaults",
      "status": "verified",
      "evidence": "`Smoke test the serve CLI` (every PR) \u2014 `--host`, `--port`, `--workers`, `--threads`, `--blocking-threads`"
    },
    {
      "id": "M3",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "Application discovery from a bare module name",
      "status": "verified",
      "evidence": "`Conformance test the ASGI bridge` (every PR)"
    },
    {
      "id": "M4",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "`--app-dir` prepended to `sys.path`, shadowing an installed package",
      "status": "verified",
      "evidence": "`Smoke test the serve CLI` (every PR) \u2014 `--app-dir`"
    },
    {
      "id": "M5",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "Usage errors exit 2, startup errors exit 1, refusals exit 78",
      "status": "verified",
      "evidence": "`Smoke test --doctor against the server's own exit codes` (every PR)"
    },
    {
      "id": "M6",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "`--help` names every documented flag; `--version` matches the release",
      "status": "verified",
      "evidence": "`Smoke test the serve CLI` (every PR) \u2014 `--help`, `--version`"
    },
    {
      "id": "M7",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "Installable wheel with no toolchain and no dependencies",
      "status": "verified",
      "evidence": "`Build and smoke test the installable wheel` (every PR)"
    },
    {
      "id": "M8",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "The aarch64 wheel built and served on arm64 hardware",
      "status": "verified",
      "evidence": "`Build and smoke test the aarch64 wheel` (every PR)"
    },
    {
      "id": "M9",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "C-ABI shared library loadable by `dlopen`/`ctypes`",
      "status": "verified",
      "evidence": "`Smoke test the C-ABI shared library` (every PR)"
    },
    {
      "id": "M10",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "The documented quickstart is executed, not asserted",
      "status": "verified",
      "evidence": "`Execute the quickstart` (every PR)"
    },
    {
      "id": "M11",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "Correct signal handling as PID 1 in a container",
      "status": "verified",
      "evidence": "`Smoke test SIGTERM delivered to PID 1 in a container` (every PR) \u2014 the wheel exec'd as PID 1 in `python:3.12-slim` (checked via `/proc/1/cmdline`, not trusted); `docker stop` must be the drain's exit 0 well inside the grace, never SIGKILL at the deadline, alone and as a supervisor reaping two workers whose exits must be clean rather than by the propagated signal"
    },
    {
      "id": "M12",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "Configuration from a TOML file",
      "status": "out of scope",
      "evidence": "flags and `M0_*` environment variables cover it; a third source is a third precedence rule"
    },
    {
      "id": "M13",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "systemd socket activation (`LISTEN_FDS`)",
      "status": "out of scope",
      "evidence": "no request for it. The old reason said `SO_REUSEPORT` covered the restart case, which is not true for anyone running `m0serve`: no flag or variable enables it (D6). What a restart here does get is the supervisor's graceful drain, so in-flight work finishes; a listener that outlives the process is a different property and nothing has asked for it"
    },
    {
      "id": "M14",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "An HTTP client in Mojo, for server-to-server calls",
      "status": "verified",
      "evidence": "`Smoke test the HTTP client` (every PR)"
    },
    {
      "id": "M15",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "Windows, musl",
      "status": "out of scope",
      "evidence": "no Mojo toolchain for either \u2014 see the platform table in README.md"
    },
    {
      "id": "M16",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "No second process: the running server is one process tree of m0serve binaries, and the wheel requires nothing",
      "status": "verified",
      "evidence": "`Execute the quickstart` (every PR) \u2014 under `--workers 2`, `pgrep -x m0serve` counts exactly a supervisor and two workers (the console script `execve`s, so no Python parent survives), and `pip show m0serve` lists an empty `Requires:`. The exact count is the self-test: a check that only looks for a broker it does not expect passes on an empty machine"
    },
    {
      "id": "M17",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "The live demo: the quickstart's shape as a public page with per-visitor channels and rate and size limits, served from its deploy image",
      "status": "verified",
      "evidence": "`Smoke test the live demo's deploy image` (every PR) \u2014 `deploy/demo/Dockerfile` built from the tree's own wheel and probed through a published port by `scripts/demo_probe.py`: a first visitor is handed a token cookie, HttpOnly and Secure exactly over HTTPS, the page names the served version, and its CSP names the page's one inline script and stylesheet by sha256 with no `'unsafe-inline'`; without the cookie the hold views answer 403 and a foreign-`Origin` upgrade is refused; one publish reaches a second stream on the visitor's channel and a stranger's stream hears nothing for a measured silence; a WebSocket frame comes back to the socket and to the streams, and the page's text-and-tab envelope comes back with its tab id naming the worker the 101's `X-Worker` names, which is how a tab learns its own socket's worker; a message over the cap is 413 and a burst meets 429 with `Retry-After` after the first LIMIT attempts and within workers\u00d7LIMIT+1; m0serve is PID 1 by `/proc/1/cmdline` and `docker stop` drains to exit 0 with held connections open. The same probe, pointed at a URL instead of the image, verifies the live deploy"
    },
    {
      "id": "M18",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "A native build flavor, `poe build-serve-native`: the build host's CPU and OS floor, for a server on a desktop Mac, beside the portable wheel. A local build or a release tarball, never a second wheel, because pip cannot select a microarchitecture",
      "status": "verified",
      "evidence": "`Smoke test Apple Silicon topology, QoS and the native build flavor` (every PR) \u2014 the native binary launches, and its `build.apple_target` names the host's generation where the wheel flavor on the same machine said m1 \u2014 or `other` where the toolchain has no predicate for what the build targeted, which is what a virtualized CPU (GitHub's macOS runners) reads; what the line always catches is the wheel binary standing in for the native one on a host that is not an M1"
    },
    {
      "id": "M19",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "A mount answered by a compiled-in Mojo handler, with no Python in its request path: `--mount PREFIX=mojo`, served by `MojoPool` threads on the mount's own lane",
      "status": "verified",
      "evidence": "`Smoke test a Mojo mount beside a Python one` (every PR) \u2014 both mounts answer and the Mojo one names the pool thread that served it; `/nativeapp` is not swallowed by `/native`; a mojo-only mounted server exits 2, because a Python host with no Python to host is asking for nothing; SIGTERM exits within 10 s with a Mojo lane live, asserted as an exit because the failure mode is a hung join rather than an error; and under eight GIL-bound connections `scripts/mojo_mount_probe.py` requires the Mojo mount's p99 to stay inside 25 ms AND to beat the Python route's by 3x, which a shared execution mode cannot do \u2014 pointing the probe's mojo-path option at a Python route is the negative arm and fails on both assertions; and beside an ASGI mount with no topology flags the Mojo mount answers within 5 s, which it never did before M20"
    },
    {
      "id": "M20",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "A mount set that some mount could not be served in is refused, never started half-served, and one that can only be served with handler threads gets them. Refusals decidable from the flags exit 2 before the bind and the fork, identically in `--doctor`: only compiled mounts, a hold mount without `--realtime` or `M0_GRANT_KEY`, a compiled mount under `--threads`, and an explicit `--blocking-threads` below the compiled mounts of a kind. A WSGI mount that an explicit `--blocking-threads` leaves with no thread on the offloaded loop exits 78 after detection, which the supervisor does not respawn. Without `--blocking-threads`, a mount set with no inline shape -- a compiled mount, or an ASGI mount beside a WSGI one -- gets the default pool even under `--workers`, and every mount at least one thread",
      "status": "verified",
      "evidence": "`Smoke test --doctor against the server's own exit codes` (every PR) \u2014 nine mount rows, each required to agree with the doctor AND to exit a named code: five 2s (two under `--workers 2`, where the old placement crash-looped to exit 1), two 78s, and two prefork mount sets that serve with no pool flag, the Mojo one with the doctor reporting `blocking_threads_source: default`. `Smoke test a Mojo mount beside a Python one` (every PR) serves `/native` from both workers under `--workers 2`. Before: a Mojo mount with no pool served, misrouted to the root application; one thread for two WSGI mounts or an ASGI mount beside a Mojo one left a mount hanging. The sizing: `test_cli.mojo:test_pool_is_default_for_mount_sets_that_cannot_run_inline`, `test_cli.mojo:test_resolve_blocking_threads_never_deals_fewer_threads_than_mounts`"
    },
    {
      "id": "M21",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "A path no mount claims is answered with the server's own 404 whatever the mounts are, never by an application: decided on the loop against the whole mount table, compiled mounts included, before a lane is chosen, so an ASGI or Mojo mount listed first, a single ASGI mount and a WebSocket upgrade all get the same answer as a WSGI-first server",
      "status": "verified",
      "evidence": "`Serve two mounted applications from one process` (every PR) \u2014 phase 2b serves three shapes with no root mount (ASGI first, Mojo first, ASGI alone) and requires `/zzz` and a WebSocket upgrade to it to answer 404 with the server's `no application mounted` body. Before: the loop sent a miss to lane 0 and only a WSGI pool thread asked the table again, so the ASGI application at `/a` answered `/zzz` with its own 200 and an upgrade with 403, a lone ASGI mount the same, and a Mojo mount listed first with its router's own 404 body -- all three fail phase 2b on a build without the check, while phase 2, whose mounts are both WSGI, passed throughout"
    },
    {
      "id": "M22",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "A Mojo mount's pool threads count on their lane and park on wake channels of their own, as the WSGI pool's do, so the elastic wake rules see a busy lane as busy: the Mojo route does not slow down as its pool grows",
      "status": "verified",
      "evidence": "`Smoke test a Mojo mount beside a Python one` (every PR) \u2014 under `M0_POOL_DEBUG=1` with four handler threads, shutdown must report four threads registered on the Mojo lane as on the WSGI one; before, the Mojo lane reported 0. Measured on `/native/probe` at 16 connections beside a WSGI mount (Apple M4): eight threads went from 202k lane wakes and 104k rps on 2.04 cores to 61k wakes and 133k rps on 1.35 cores, the one-thread figures, and the route now beats the WSGI route beside it per core"
    },
    {
      "id": "M23",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "Mounts under `--threads` on free-threaded CPython: WSGI mounts serve from their own lanes in every loop with their prefixes intact, a slow mount does not stall another, and SIGTERM ends every loop; an ASGI mount is refused with 78 on this toolchain (modular/modular#5726) rather than served",
      "status": "verified",
      "evidence": "`py-canary` (weekly) \u2014 phase G runs `poe smoke-mounts-threads` on 3.14t: Django and Flask mounted under `--threads 2 --blocking-threads 4`, the Flask mount's `SCRIPT_NAME` and `url_for`, `scripts/hybrid_isolation.py` measuring the Flask mount while four blocking Django views hold the Django lane (50 ms budget, a shared lane lands near 2 s), two clean loop exits on SIGTERM, and the ASGI refusal with its message and `--doctor`'s 78. On a GIL-enabled interpreter the same task checks only that a mounted `--threads` server is refused. It replaces `smoke-hybrid`'s phase 3t, which served ASGI mounts under `--threads` and so could not pass on the only interpreter where it ran, and which no scheduled job ran"
    },
    {
      "id": "M24",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "A mounted server under `--threads` on a GIL-enabled interpreter is refused with 78, never served, as an unmounted one is",
      "status": "verified",
      "evidence": "`Smoke test mounts under the threaded mode's guard` (every PR) \u2014 Django and Flask mounted under `--threads 2` must exit 78 on CI's GIL-enabled 3.13; the same task is M23's full check on 3.14t"
    },
    {
      "id": "M25",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "A Mojo or hold mount's lane is not held to one thread by the GIL lanes' progress rule: its threads never attach, so the loop wakes a parked sibling once the head of its ring has waited `POOL_FREE_WAKE_AGE_NS` since its push, whether or not the ring is moving, while `submit` keeps the elastic rule (`M0_POOL_PARALLEL=0` restores the GIL rule on every lane)",
      "status": "verified",
      "evidence": "`test_offload.mojo:test_a_gil_free_lane_wakes_a_sibling_behind_a_draining_ring` (every PR) \u2014 a GIL lane and a GIL-free lane in one pool with the same history (a busy thread, a parked one, a ring that moved): only the GIL-free lane's sibling is woken, at 50 ms on a clock whose GIL threshold is 100 ms, and neither push wakes anyone; reverting the count from the push, the lower threshold, the elastic submit, the knob or the mark each fails it. `smoke-mojo-mount`'s pool-debug phase asserts the wiring: the Mojo lane prints as GIL-free and the WSGI lane does not. Measured on `/native/search` beside a WSGI mount, four threads, eight connections (Apple M4): 15k rps on 1.2 cores at full selectivity and 36k on 1.4 at a quarter, where the eager wakes served 42k on 4.2 and 78k on 3.5 but cost the trivial `/native/probe` 21 %; with this rule 43k and 76k, and the price is paid by the lightest routes: the probe 195k on 1.57 cores against 199k on 1.51, the sel=5 search 111k against 115k"
    },
    {
      "id": "M26",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "A Mojo application ships as an image with no interpreter and no toolchain in it: the pinned toolchain compiles it in a builder stage, the runtime stage carries the binary and the Mojo runtime libraries beside it, and the binary is PID 1 under `docker stop`",
      "status": "verified",
      "evidence": "`Smoke test the Mojo demo's deploy image` (every PR) \u2014 `deploy/mojo/Dockerfile`, one file for every Mojo app with `APP` naming its directory, built for `apps/blobs` from the tree and probed through a published port by `scripts/mojo_image_probe.py`: `/proc/1/cmdline` read INSIDE the container is exactly `/app/server`, no `python3`, `python`, `python3.x` or `libpython` exists anywhere in the image, and `docker stop` is the drain's exit 0 inside 10 s. The target CPU follows the docker daemon's architecture, so on CI's runner this is an x86-64 build (`x86-64-v2`), the only one CI makes of a Mojo app. `sabotage-mojo-image` (pre-release) builds a shell at PID 1, a stop signal the server ignores, an interpreter added after the image measured itself and one before (which the build refuses), and the unrelocated binary shipped (which the build refuses), and each fails where it should. Measured on `linux/arm64` (colima, Apple M4): the blobs image is 29.3 MB compressed and 102.1 MB unpacked, 3.0 MB of it the app, against 60.3 MB and 184.6 MB for the Django demo's image from the 1.4.0 wheel; on CI's x86-64 runner the blobs image is 80.1 MB unpacked. Until 2026-09-18 this row set the hello image's COMPRESSED size (29.2 MB) against the Django demo container's MEMORY (74 MiB, deploy/demo/README.md), which are not the same measure; `deploy/mojo/README.md` records each figure with its architecture and what it counts. Pre-release until the same day, on a build cost that had not been measured"
    },
    {
      "id": "M27",
      "section": "M",
      "category": "M. Deployment and operations",
      "capability": "The Mojo demo served from its deploy image: the producer thread steps and streams inside the image, the page's claims about the image are measurements the image made of itself, and `docker stop` ends a held stream from the server with exit 0",
      "status": "verified",
      "evidence": "`Smoke test the Mojo demo's deploy image` (every PR) \u2014 the image's last layer measures itself (unpacked bytes of the filesystem and of `/app`, and that no interpreter is anywhere in it, failing the build if one is) into `/app/about.json`, which the app reads through `M0_IMAGE_FACTS`; the probe re-measures with `du` as root and requires agreement within 1 %, the version to be `pyproject.toml`'s, the target CPU the one asked for, `/about` to be the file verbatim, and the page's footer to carry exactly its version, its no-Python clause and both sizes as the app formats them. `scripts/blobs_probe.py serve`, `smoke-blobs`' main run, then passes through the published port, 100 streams are held and answered, and a stream that has received a frame is ended BY THE SERVER on `docker stop` -- exit 0 within 10 s, nothing abandoned in the log. `sabotage-mojo-image` (pre-release) builds a size written rather than measured, a footer sizing the app as the image, a page rendered without its footer, an `/about` that is not the file, and a producer never told to stop, and each fails in the phase that names it. The same probe, pointed at a URL instead of an image, is what the `deploy-blobs` job runs against `blobs.m0serve.dev` after a release: `/about` naming the version, the footer, whole-state frames and a producer stepping"
    },
    {
      "id": "N1",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A server-rendered fragment app: one URL answers a bare fragment under `HX-Request: true` and a whole document without it, `Vary: HX-Request` on both; a urlencoded form round-trips a multi-byte title and keeps every value of a repeated checkbox key; a markup title renders as text; a body that is not a form is refused; 405 with a correct `Allow`",
      "status": "verified",
      "evidence": "`Smoke test the fragment notes app` (every PR) \u2014 `apps/fragment_notes`, the same resource `apps/notes_api` serves as JSON"
    },
    {
      "id": "N2",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A view is a function the URL table names: registration assigns the handler id, a path in a method it does not take is 405 with `Allow`, an unmatched path is 404, and there is no fallthrough for a route to reach the wrong view. A view registered as reading receives the state borrowed, so a write in it does not compile (`poe sabotage-views`, inside `test-all`, compiles the counter-examples and insists they are refused)",
      "status": "verified",
      "evidence": "`test_views.mojo:test_wrong_method_is_405_with_allow` (every PR)"
    },
    {
      "id": "N3",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A fragment names itself: `Fragment` writes its root id once and `swap` generates the attribute that targets it from that same id; `Html.attr` owns the attribute delimiters and escapes the value, `text` escapes, `raw` says so by name",
      "status": "verified",
      "evidence": "`test_html.mojo:test_fragment_emits_its_id_once_and_targets_it` (every PR)"
    },
    {
      "id": "N4",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "`Vary` accumulates: a response that varies on two request headers names both, and a name is never repeated",
      "status": "verified",
      "evidence": "`test_reply.mojo:test_vary_appends_rather_than_overwrites` (every PR) \u2014 the overwrite it replaced kept whichever header was set last, unnoticed while nothing set `Vary` twice"
    },
    {
      "id": "N5",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "Reverse URLs from the route pattern: `url_for(PATTERN, params...)` fills a pattern's `:name` segments in order, percent-encodes each value so it stays one segment, and raises on an arity mismatch; every route a router registers reverses to a path that matches back to the same handler",
      "status": "verified",
      "evidence": "`test_router.mojo:test_every_registered_route_reverses_and_matches` (every PR) \u2014 the pattern is a `comptime` constant given to both `add` and `url_for`, so a misspelled route is a compile error rather than a dead link"
    },
    {
      "id": "N6",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "Urlencoded form bodies: `form(req)` decodes `application/x-www-form-urlencoded` into an ordered multimap that keeps every value of a repeated key, and is empty for any other content type so a JSON body is never read as a form; it decodes every value exactly as the query-string parser does, proven by one table run through both",
      "status": "verified",
      "evidence": "`test_form.mojo:test_decodes_exactly_as_the_query_parser_does` (every PR) \u2014 kept as a separate loop from `URI.parse` on purpose: that one is a last-wins dictionary by contract, and sharing the loop would force one of the two to change"
    },
    {
      "id": "N7",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "One renderer, two transports: `Fragment[V]` takes the frontend vocabulary as a type parameter, `Htmx` and `Datastar` \u2014 the two the layer ships; N21 is an application's own \u2014 each spelling their attributes in one struct, so the same renderer serves both; a `Fragment[Datastar]` is one line and goes out verbatim as the single `elements` line of a `patch_elements` frame, needing no selector because it carries its own id; and a request carrying `Datastar-Request: true` is answered the bare fragment as `text/html`, which Datastar morphs by that id",
      "status": "verified",
      "evidence": "`test_fragment_frame.mojo:test_a_fragment_goes_out_verbatim_as_one_elements_line` (every PR) \u2014 `apps/datastar_todo`'s list is that renderer, and `smoke-todo` greps its output out of a live stream's frame; the header arm is `test_fragment.mojo:test_a_datastar_action_gets_the_bare_fragment` plus a `Datastar-Request` request in `smoke-fragment-notes`"
    },
    {
      "id": "N8",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A history restore is a page, and so is a boosted navigation, for a client that does not say which it wants (htmx 2; N22 is the client that does): `HX-History-Restore-Request: true` or `HX-Boosted: true` beside `HX-Request: true` answers the whole document, because htmx swaps the answer's body into the page it is rebuilding (or into the body it targets) and a bare fragment there is a page with no head, or a body that is one section; every answer's `Vary` names all five headers the decision reads",
      "status": "verified",
      "evidence": "`Smoke test the fragment notes app` (every PR) \u2014 `test_fragment.mojo:test_a_history_restore_is_a_page` is the unit form; the request shape is htmx 2.0.4's own `loadHistoryFromServer`, which sets both headers"
    },
    {
      "id": "N9",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A table knows where it is mounted: `Views[S](Mount(prefix))` registers every route under the prefix, `Mount.url_for` reverses to those same paths, and `PoolContext.prefix` carries the prefix from the pool's own lane table, so a handler on a mounted lane matches exactly what the loop sends it and renders links that carry the prefix. Gated the way `smoke-hybrid` gates Python's `reverse()`: a link the mount rendered is followed, and the same link without its prefix is followed too and answered 404 by the root application",
      "status": "verified",
      "evidence": "`Smoke test a Mojo mount beside a Python one` (every PR) \u2014 the unit forms are `test_router.mojo:test_a_mount_prefixes_every_reversed_url` and `test_views.mojo:test_a_mounted_table_matches_under_its_prefix_only`; the mount is `m0serve`'s own `MojoMount`, now a `Views` table whose index renders its routes"
    },
    {
      "id": "N10",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "Elements as expressions: `el(tag, attrs, children...)`, `void`, `attr`, `flag` and `text` compose as strings with the builder's escaping \u2014 the same list from both tiers is byte-identical \u2014 and `Fragment[V].el(tag, verb, url, ...)` spells the swap for the vocabulary from the tag it is given once, so a renderer reads as its markup with each escaping context named at the hole; `apps/fragment_notes`'s list is written this way beside its detail view in the builder",
      "status": "verified",
      "evidence": "`test_html.mojo:test_elements_compose_as_expressions_with_escaping_named` (every PR) \u2014 the swapping form is `test_html.mojo:test_fragment_el_spells_the_swap_from_the_tag_it_is_given`; `smoke-fragment-notes` did not change when the list moved onto the tier"
    },
    {
      "id": "N11",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "An SSE hold from a Mojo mount: a view on a mounted `Views` table returns the two instruction headers a Django view returns (`M0-Hold: stream`, `M0-Channel`), the pool thread rewrites the response into the stream's head and sends the loop the same `h` frame a WSGI pool thread sends before completing, the loop subscribes the slot in the registries it drains, and a publish from Python reaches the Mojo-held stream with its id \u2014 the client's `Last-Event-ID` honoured, comment heartbeats kept, no chunked framing, the slot released when the client leaves, and the head answered while every Python thread is busy; a streaming response that is not a hold is still refused 409, and without `--realtime` the head is served as an ordinary response, as a Django hold view's is",
      "status": "verified",
      "evidence": "`Smoke test an SSE hold from a Mojo mount` (every PR) \u2014 the unit form is `test_mojo_pool.mojo:test_a_hold_sends_the_loop_its_frame_and_completes_with_the_head`, and `poe sabotage-pool` reverts both the frame's send and the refusal; the mechanism is [hold-from-a-mojo-mount](notes/hold-from-a-mojo-mount.md)"
    },
    {
      "id": "N12",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A Datastar form end to end: `Fragment[Datastar]` on a `<form>` emits the submit action with `{contentType: 'form'}`, `form(req)` reads what the browser sends, and the broadcast morphs the answer into every tab; gated by a smoke that posts the form and greps the frame, and a browser run that submits it for real, since the form arm is where a wrong spelling fails silently",
      "status": "verified",
      "evidence": "`Smoke test the Datastar todo demo` (every PR) \u2014 posts the rename form urlencoded and greps the frame, greps the page for the form arm's wire spelling, and refuses a JSON body posted to the same route; the arm in a real browser is `browser-datastar-form` (pre-release): Chromium presses Enter in the rename form and the pinned bundle posts the fields urlencoded, a bound field's action posts the signal store as JSON (D21), and a second tab morphs"
    },
    {
      "id": "N13",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "Sessions and CSRF behind a login: a stateless signed cookie `v1.<kid>.<exp>.<subject>.<tag>` \u2014 HMAC-SHA256 over the rest, the tag checked in constant time, the expiry against the host clock, a retired key ending every session under it \u2014 emitted through `ResponseCookieJar.add_raw` with `HttpOnly; SameSite=Lax; Path=/`; a CSRF token derived from that tag, carried as a hidden field by a POST and as an `X-CSRF-Token` header by a DELETE (N22), read from the body or that header and never from the query string, a header that is present deciding; a guard that is an early return, answering 303 to a navigation and 401 carrying the login fragment to a swap; and a server that refuses to start (78) without its key or its password",
      "status": "verified",
      "evidence": "`Smoke test the fragment notes app` (every PR) \u2014 every arm on the wire, with the forgeries signed by `scripts/notes_session.py` so the verifier is never checked against itself; the unit form is `test_session.mojo:test_a_cookie_the_issuer_signed_names_its_subject` against the same issuer's vectors, and `poe sabotage-notes-login` (pre-release) reverts the tag check, the expiry, the token, the guard and the cookie attributes one at a time. The arm curl cannot judge is `browser-notes-login` (pre-release): Chromium signs in, writes and deletes against htmx 4.0.0, the POST carrying its token in the body and the DELETE in a header with no `csrf` in its URL. The design is [a-login-on-the-notes-app](notes/a-login-on-the-notes-app.md)"
    },
    {
      "id": "N14",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A Mojo mount from somewhere else: an application supplies its own `MojoMount` as a module named `m0serve_mount` in a directory of its own, and `poe build-serve` with `M0SERVE_MOUNT_DIR` naming that directory builds the same `m0serve.mojo` into a binary that serves the application's routes \u2014 its path parameters, its per-thread state, its prefix-aware links \u2014 beside a Python mount, with the demo mount's routes gone rather than answering too; the directory REPLACES the default on the include path, because with both present the first root wins without a word, and a directory with no mount module is refused by the build before anything compiles",
      "status": "verified",
      "evidence": "`Smoke test a Mojo mount supplied by module` (every PR) \u2014 sabotaged by keeping the default mount directory on the path beside the fixture's, which built cleanly and served the demo, and failed the smoke's first assertion; that the conformance survives leaving the entry file is measured by `Smoke test a Mojo mount beside a Python one` passing with the demo mount in the module; the design is [a-mount-from-somewhere-else](notes/a-mount-from-somewhere-else.md)"
    },
    {
      "id": "N15",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "Periodic work off the event loop: an application with a simulation cadence runs it on a thread of its own and publishes each step through the `BroadcastBus` with `skip_worker = -1`, the loop draining it into `sse_peer_frame` and paying only that; the bus is joined at ONE worker too, because here it is the thread-to-loop channel rather than only worker-to-worker, and publishing with nothing draining fails quietly; step ids arrive contiguous, so a cadence the best-effort bus cannot carry is a failure rather than a silence; a trivial request is unaffected while a 200ms step runs off the loop and waits out that step when the identical work is moved onto `tick` (`M0_SIM_ON_LOOP=1`, the negative arm, which also asserts the step ran there at all); a step still running at SIGTERM is joined within the drain rather than hanging it; and under `M0_WORKERS=2` streams held by EVERY worker receive the steps, which needs the thread to publish to every worker's channel (the app shipped handing it `bus.write_fds[0]` alone) and the workers to share accepts so the held streams span both, each naming its worker in `x-worker`",
      "status": "verified",
      "evidence": "`Smoke test periodic work off the event loop` (every PR) \u2014 `apps/sim_loop`, whose two arms are the same step function reached two ways, and which runs on the Mojo host (E21), so the thread, its publish and its join are the host's `Producer` now and `sabotage-host` breaks those rules against `smoke-host`. Before the host, this gate was sabotaged by publishing with `skip_worker = 0` instead of `-1`, which delivers to nobody and fails the first assertion; by handing the thread `bus.write_fds[0]` alone, which fails the two-worker phase's frame count; and, over that broken fan-out, by `M0_ACCEPT_SHARE=0`: the bare race put all four streams on one worker in 1 of 3 macOS runs, where the frame count passes and only the phase's worker count fails it. The cost the row exists to avoid is the duty cycle: retention tracks `1 - duty` and the work transfers into p99 about one for one, which is what `HTTPService.tick`'s docstring now says"
    },
    {
      "id": "N16",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A live world the page cannot hold: `apps/blobs`, a `Views` table that also owns the four SSE hooks, whose producer thread steps up to sixteen metaballs at 10 Hz \u2014 the field sampled on a 192 \u00d7 192 grid, its outlines traced by marching squares chained on grid-edge identity, closed by a zero border, holes dropped, each resampled to 48 vertices and kept in the slot its predecessor held \u2014 and publishes each step as one full-state `patch-signals` frame of underscored signals \u2014 every slot, a `polygon()` of exactly 48 vertices inside the stage, one winding, in the vertex order nearest the slot's previous polygon (a slot's first starting topmost), a new shape first drawn as its parent's outline or a seed of itself and an ended one shrinking away before its slot hides \u2014 to every worker's bus channel; the stream sends the newest state at open (I24); the producer pauses with nobody watching and slows to 2 Hz after an idle stretch; a click is validated and capped on the loop (400 for a body without numeric `x` and `y`, 429 past four a second per connection), clamped so a lone blob stays clear of the walls, handed to the producer over the pre-fork shared page, and reaches every stream; seventeen blobs evict to sixteen; the kernel leaves no contour open; a drain with a stream held exits 0 without abandoning the producer; and it runs on the Mojo host (E21), so under `M0_WORKERS=2` streams on both workers receive every step, a click posted to worker 1 reaches the producer in worker 0 over the shared page, and the pause waits for the viewers summed across workers",
      "status": "verified",
      "evidence": "`Smoke test the blobs demo` (every PR) \u2014 three servers from one binary, each on its own port, the last with two workers, where the streams must span both workers or the phase fails as vacuous; also times a trivial route while steps run and records the step time, the most shapes drawn, the largest frame and the bytes per viewer per second. Its probe also checks consecutive frames: no slot twists (its vertices move no more than the nearest rotation's, to the wire's 0.1 % rounding), none hides without having shrunk, and none appears but as a copy of a shape in the frame before or a seed under 0.3 of what it grows into. `poe test-apps` (inside `test-all`) holds the kernel's contract, every trace checked against the one before, and its rules \u2014 the prototype's own layout traces to 1190 segments and four cycles, one a hole it drew and the kernel drops; sixteen blobs pressed against a wall still close; a moving shape keeps its slot and a new one never inherits a slot that just emptied; a peanut whose highest point passes between its lobes keeps its vertex order (topmost every step dragged every vertex around the outline: 64 of 1,758 slot steps on 1.5.0's live stream); a merge becomes the merged outline then shrinks, a split starts as its parent, an ended shape shrinks and a drop grows from a seed \u2014 plus the frame and the drop box. The browser half is `browser-blobs` (pre-release): Chromium requires a patched `_`-signal to drive `data-style:clip-path`, a click to post exactly `x` and `y` at the point clicked with a second tab gaining the blob, and both tabs to reopen the stream and draw the new world after the server restarts, which Datastar's default retry does not do after a clean close. `sabotage-blobs` (pre-release) breaks thirty-one rules one at a time \u2014 seventeen against the smoke (among them a twist and both kinds of pop reaching the wire, the pause counting worker 0's viewers alone, and worker 1 holding a board of its own, whose click never crosses), fourteen kernel rules against the unit tests (complementary cases wound alike, chaining on rounded coordinates, no border, holes kept, vertex 0 not re-chosen or rotated past the topmost, no slot matching, a just-emptied slot reused, a slot restarting at its topmost vertex every step, a shape hidden at once, a new shape drawn whole, a split drawn whole beside its parent, an absorbed shape skipping the merged outline, an unclamped drop) \u2014 and requires the gate to fail for each"
    },
    {
      "id": "N17",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "One list, two workers: `apps/datastar_todo` on the Mojo host serves `M0_WORKERS=2` over one SQLite file, each worker restoring the same journal and joining its stream to the bus, so a mutation on either worker reaches every tab; and a mutation holds SQLite's write lock from its change until its frame is published (`BEGIN IMMEDIATE` ... `COMMIT`), so frames are numbered in the order their renders saw the data and the newest frame a tab keeps is never missing a change an older one had",
      "status": "verified",
      "evidence": "`Smoke test the Datastar todo demo` (every PR) \u2014 `scripts/todo_probe.py two`: streams that must span both workers (or the phase is vacuous), twenty-five adds at once from a connection held by each worker, every stream converging on all fifty, the page agreeing, and the persisted broadcast log read from the database file, each frame required to hold every todo of the frame before it. The race lasts microseconds, so the app's `M0_TODO_RENDER_PAUSE_MS` (5 ms in the gate) widens it: a build without the lock failed that check 5 of 5 rounds, the build with it passed 5 of 5; without the pause the unlocked build passed 10 of 10, which is why the gate uses it"
    },
    {
      "id": "N18",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A `Views` table and its state are a host application with no handler of their own: `ViewsApp[S]` serves a state conforming to `ViewState` (`make`, `urls`), forwarding `func` to `dispatch` and `before_request` to `answer_on_loop`; and a state that lives in one process declares `max_workers`, so `M0_WORKERS` above it is refused with 78, naming the variable, before anything is bound, instead of serving workers that each hold a different copy",
      "status": "verified",
      "evidence": "`Smoke test the fragment notes app` (every PR) \u2014 `apps/fragment_notes` is `serve[ViewsApp[NoteStore]]`, its whole wire contract unchanged, and `M0_WORKERS=2` must exit 78 naming the variable without the listening banner (the app previously ignored the variable and served one process); the adapter's forwarding is `test_host.mojo:test_a_views_app_answers_for_its_state`"
    },
    {
      "id": "N19",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A route's placement is a property of the route: `add_read` and `add_write` take `on_loop=True`, which keeps the route in the table (one `Allow`, one `url_for`, one 404) and has `answer_on_loop(req, state)` \u2014 what `ViewsApp.before_request` and `ViewService.before_request` call \u2014 answer it ON THE LOOP with the loop instance's state, reads borrowed and writes `mut`, before the request becomes a pool job; a view that raises there is 500, since the hook cannot raise; the stateless `answer_on_loop(req)` declines it and `dispatch` answers it one round trip later, which is what `m0serve`'s loop does, its handler holding no Mojo table (D32); `add_loop` stays the stateless form both hosts answer on the loop",
      "status": "verified",
      "evidence": "`test_views.mojo:test_an_on_loop_route_is_answered_with_its_state_before_dispatch` (every PR) \u2014 its neighbours cover the stateless decline, the 500 and `ViewService`; `sabotage-host --only views` (pre-release) answers the on-loop read without its state and insists the test fails. Where a stream open goes under a pool, and why: [the-ramp-test](notes/the-ramp-test.md)"
    },
    {
      "id": "N20",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "One views module, two hosts: `apps/ramp/views.mojo` \u2014 a `Views[Ramp]` table, its views and its state, built from the prefix alone \u2014 compiles into `m0serve` as `MojoMount` under `--mount /x=mojo` (`M0SERVE_MOUNT_DIR`, plus `M0SERVE_INCLUDE` for the application's own module root) and into a host binary under the same `Mount(\"/x\")`, and the two answer a fixed request set under the prefix byte-identically apart from `Date` (the index, `now`, the compute route, a 404 inside the prefix, a 405 with `Allow`, an `OPTIONS` 204), follow the same rendered link, answer what each host owns outside the prefix (the Python application on `m0serve`, the table's 404 on the host, and `/xapp` never swallowed by `/x`), and keep `/x/now` answered while a connection holds `/x/slow` on a lane of two \u2014 on `m0serve` from the lane's other thread (D32), on the host from the loop \u2014 where the host with no lane fails the same bound; and with the lane FULL (two connections on two threads) the host's loop route still answers, while `m0serve`'s waits for a thread \u2014 D32's divergence, measured on every run rather than argued",
      "status": "verified",
      "evidence": "`Smoke test one views module on two hosts` (every PR) \u2014 `scripts/ramp_probe.py` diffs status, headers and body per request across the two ports (only `Date` differs, measured) and follows the index's links on each; the placement bound is 100 ms, sim_loop's, and `M0_BLOCKING_THREADS=0` on the host is the negative arm that must reach it (1 ms and 0 ms on the two hosts with one loader, 385 ms on the bare loop, on an M4); the full-lane arm bounds the host (0 ms) and records `m0serve` (196 ms). The design and the measurements: [the-ramp-test](notes/the-ramp-test.md)"
    },
    {
      "id": "N21",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A vocabulary an application defines: `Vocabulary` is open to a conformance written in an application's own source, through the `.mojoc`, using nothing spelled with an underscore \u2014 `Html.open_kind()` says which element is open, and a defaulted static `verbs()` names the verbs the library takes; the layer, not the conformance, refuses a verb outside that list before `swap` runs, so a vocabulary that checks nothing still refuses a typo and one with a sixth verb (htmx 4's `query`, which is how the built-in `Htmx` takes it) is allowed it while one left on the default five, `Datastar`, still is not",
      "status": "verified",
      "evidence": "`test_html.mojo:test_the_layer_refuses_a_verb_a_conformance_never_checked` (every PR) \u2014 `poe check-app-vocabulary`, inside `test-all`, is the outside half: an htmx 4 vocabulary compiled from a directory that is not under the repository root, asserting `hx-swap=\"outerMorph\"` and `hx-query` by exact string in both tiers and `open_kind()`'s four answers through a second, synthetic vocabulary (htmx picks its own event, so `Htmx4` never asks), then read by `hxlint`, vendored from hx-flask and generated from htmx 4's source tree, which the gate first makes fail three ways; `test_html.mojo:test_a_vocabulary_names_its_own_verbs` and `test_html.mojo:test_a_conformance_reads_the_element_kind_without_an_underscore` are the other two arms. The htmx 4 APPLICATION this row once disclaimed \u2014 a DELETE's fields ride the query string there, CSRF token included, which no lint sees \u2014 is N22"
    },
    {
      "id": "N22",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "The built-in `Htmx` vocabulary and `page_or_fragment` speak htmx 4: `HX-Request-Type` DECIDES when it says `partial` (the bare fragment) or `full` (the whole document) \u2014 a v4 history restore carries `full` and no `HX-Request` at all, and a v4 boosted element with its own target says `partial` beside `HX-Boosted: true` and gets the fragment, the one place the two majors need different answers to the same older headers \u2014 while a request without it is read by N8's rule unchanged and a value that is neither word falls through to it; `Vary` names it fifth; `Htmx` takes htmx 4's sixth verb, `query`, and spells a swap in the same three per-element attributes, which explicit inheritance leaves intact; and a DELETE's CSRF token travels as a header, because htmx 4 sends a DELETE's fields in the query string with no setting to change it, so the notes app's delete form holds no field and carries `hx-headers`",
      "status": "verified",
      "evidence": "`Smoke test the fragment notes app` (every PR) \u2014 the four request shapes on the wire, the shell loading 4.0.0 by exact URL with no `htmx-config` left behind, the delete form carrying this session's token as `hx-headers` and no `csrf` field, and a DELETE refused with its token in the query, absent, another session's, or wrong in the header beside a right field; `test_fragment.mojo:test_htmx_4_says_which_it_wants_and_is_taken_at_its_word` is the unit form, with the restore, the boosted partial and the fall-through beside it. What the bundle actually SENDS is `browser-notes-login` (pre-release): Chromium against 4.0.0 records `HX-Request-Type: partial` on both writes, the token header on the DELETE and nothing in its URL, and a 401 swapped into the list's place, each arm shown to fail first (the header reverted, the field restored, the 2.0.4 bundle); `poe sabotage-notes-login` (pre-release) reverts both header branches, the fifth `Vary` name and the token rules. Not claimed: `hx-boost` or pushed history clicked through in a browser \u2014 no app here uses either, so those two shapes are gated as requests, from headers measured in Chromium. The record is [the-layer-moves-to-htmx-4](notes/the-layer-moves-to-htmx-4.md)"
    },
    {
      "id": "N23",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "The `m0` wheel carries the framework as SOURCE, mapped from the repository's own manifest: `packaging/m0/`'s build hook force-includes every file `git ls-files` names under the five trees an application compiles against (`m0-core/src`, `m0-http/src`, `m0-http/lightbug_http`, `m0-http/m0_host`, `m0-datastar/src`) into `m0/_mojo/<import name>/`, so one include root resolves all five with no `.mojoc` anywhere and nothing is staged or copied on disk; an untracked file cannot ship and a tracked one cannot be dropped, with no exclude list; the three tools a release build runs (`relocate.py`, `bundle_artifact.py`, and the `binfmt.py` both import) ride unedited; `m0/_build_info.json` records the release, the tree's commit, and `gated_mojo` READ from the root pyproject's exact `mojo==X` \u2014 the pin every gate ran on \u2014 never written by hand; the wheel is `py3-none-any`, declares no dependency (D39), and is versioned apart from the repository (D43)",
      "status": "verified",
      "evidence": "`Smoke test the m0 wheel` (every PR) \u2014 opens the wheel and requires the names under `m0/_mojo/` to EQUAL the mapped manifest, spelled a second time in the gate so the recipe's table and the gate's must agree, every file byte-identical to the tree, the fork's `LICENSE` present, the tools byte-identical to `scripts/`, no `.mojoc` or `__pycache__`, `gated_mojo` equal to the root pin, the commit equal to `HEAD`, and no `Requires-Dist`; then builds a second wheel with an ignored decoy file planted in `m0-http/src` and requires it not to ship, because with nothing untracked in the tree a directory walk and the manifest make the same wheel. `poe sabotage-m0-wheel` (pre-release) drops a tree, walks the directory instead of the manifest, drops `binfmt.py`, writes the pin by hand, adds the dependency, and ignores the local version label. The design is [the-m0-wheel](notes/the-m0-wheel.md)"
    },
    {
      "id": "N24",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "An application outside this repository builds against the installed wheel: `m0 build` compiles `src/server.mojo` with ONE include root, the wheel's `m0/_mojo`, and RENAMES the result onto `bin/server`, never writing over a binary that may be running, so a rebuild under a live server leaves it serving and a build that fails leaves `bin/` byte-identical; `m0 test` is `mojo run` per `test/test_*.mojo` with the framework's root and `src/`, serial, output untouched, exit 1 naming the files that failed, and a run that found no test file is refused (78), not passed; a release build compiles for the platform's baseline CPU (`build-serve`'s table; `native` beside it is exit 2), relocates, and bundles the Mojo runtime into `dist/`, which then runs with the build venv gone; `m0 include` prints the root. Exit codes are a closed set \u2014 0, 1 the tool m0 ran failed, 2 the command line, 78 a refusal (D40)",
      "status": "verified",
      "evidence": "`Smoke test the m0 wheel` (every PR) \u2014 in a temporary directory laid out as a user's project, `.venv` beside `src/`: `apps/fragment_notes/server.mojo` copied in and built, served, `/health` and the login page read; one string literal changed and rebuilt WHILE the old process serves, which must survive, still serve the old literal, keep its inode byte-identical (read through a hard link), and be replaced by a binary serving the new one; a syntax error leaves the binary's hash unchanged and no `.server.next`; a copied `apps/blobs` test that imports the framework and the application's own modules passes, a failing file beside it is exit 1 and named, no files is the 78 sentence; `dist/server` searches only `@loader_path`/`$ORIGIN` and answers its doctor from another directory with the venv moved away. Build and test seconds are recorded, never gated. Not claimed: that a release build compiled for the baseline rather than the host \u2014 the artifact works where it was built, which is where the gate runs \u2014 nor that the gate holds the RENAME as a mechanism on macOS, where mojo 1.1.0's link step replaces an existing output itself and a reverted rule passes (measured; the outcome is what is asserted)"
    },
    {
      "id": "N25",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "The toolchain is a checked PAIR and every refusal is a sentence: `checks.py` holds ONE ordered list \u2014 `platform`, `mojo-installed`, `mojo-gated`, `max-gated`, `c-compiler`, `project` \u2014 that every mojo-running command reads to its first failure and `m0 doctor` reads whole, each failure exit 78 with one `m0: detail (fix)` line and nothing run. m0 compares the `mojo` distribution in its OWN environment to `gated_mojo` by string equality, with no override (D39), and runs `<sys.prefix>/bin/mojo`, never `PATH`'s; a foreign m0 in a project that has its own `.venv` is told `uv run m0`. The C-compiler check is the name `cc` \u2014 the only one mojo 1.1.0 looks for, so `gcc` without it is refused by name \u2014 plus a one-line link with `-lm`, because a `cc` that cannot link otherwise fails after the whole compile; macOS asks `xcode-select -p` first. `m0 test` skips it: `mojo run` links nothing",
      "status": "verified",
      "evidence": "`Smoke test the m0 wheel` (every PR) \u2014 with no network: a `mojo` 9.9.9 distribution zipped on the spot, whose `mojo` script drops a marker, gives the pair sentence verbatim from `build`, `test` and the doctor's `mojo-gated` check, marker absent; that venv's m0 run from the real project gives the prefix sentence; a venv with no mojo, a directory with no `src/server.mojo`, `PATH` stripped to the venv, a `gcc` with no `cc`, and a `cc` that prints `cannot find crti.o` each give their sentence whole, 78, and an untouched `bin/server` \u2014 while `m0 test` under the stripped `PATH` still passes; and EVERY real build and test in the gate ran with the stub `mojo` first on `PATH`, the marker absent at the end and the stub then shown runnable, so its absence means something. `packaging/m0/tests/test_m0.py`, run by the same step under the installed interpreter, holds what no CI leg can be put into: the platform verdicts. `poe sabotage-m0-wheel` (pre-release) reverts each check with the unit phase OFF, so the arm and not the unit test is what must fail"
    },
    {
      "id": "N26",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "`m0 doctor` asks the toolchain's question and then the application's: every check in the list, then `bin/server` run with its own doctor flag, whatever followed a bare double dash, and the environment untouched; the host's report (E31) is taken from the LAST stdout line and required to open `\"m0_host\":\"1\"` \u2014 another format number is refused by name, `m0 build` the fix. It exits with the first failed check's code, else the application's own, else 0; no build is green, a binary older than `src/` is reported `stale` and never failed, and an application whose own `main` refuses before `serve` prints no report, which is recorded with its words. Asked for JSON, one object is the last stdout line: a format number `\"m0\":\"1\"`, `ok`, `exit`, `versions` (the release from the wheel's metadata, the gated and installed mojo, the framework tree and commit), `paths`, `checks`, and `app` with the host's report verbatim",
      "status": "verified",
      "evidence": "`Smoke test the m0 wheel` (every PR) \u2014 the report's keys in order, green with the host's report and its `topology` inside and `paths.mojo` the project venv's; `stale` true after an edit and the exit still 0, false after a build; a worker count of zero handed through exits 78 with every m0 check passing and the failed check in the HOST's report, so the code came through the binary; fragment_notes run without its key exits 78 with `report: null` and the app's sentence in `output`. The unit file holds the format refusal and the exit order"
    },
    {
      "id": "N27",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "`m0 new NAME` writes an application, from a closed set of two templates: `views`, a server-rendered list swapped in place by htmx 4, and `live`, a producer pushing full-state Datastar frames to every tab (D44). It needs no toolchain and no network \u2014 it runs through `uvx` before anything is installed \u2014 and copies files the wheel carries, replacing three tokens by plain string replacement: there is no template engine. NAME, or a path's last segment, is the directory, the project and the deploy's app name at once (lowercase letters, digits and hyphens, a letter first, at most 40), and a name that cannot be all three, or a target that exists and is not empty, is exit 2 with one sentence and nothing written. What is written is a closed manifest \u2014 `src/` with `server.mojo` importing its siblings by bare name, `test/`, a `pyproject.toml` with no build system that pins BOTH `mojo` and `m0` exactly in its dev group, `AGENTS.md` with `CLAUDE.md` pointing at it, an executable `smoke.sh`, a workflow, and `deploy/` \u2014 after which the user's own `uv run m0 build` makes the venv and the binary, `uv run m0 test` runs the template's tests, and the server answers on the wire",
      "status": "verified",
      "evidence": "`Smoke test the m0 scaffold` (every PR) \u2014 per template, outside the tree: written by `uvx` offline with `PATH` holding uv's directory and the system's alone; the written tree EQUAL to the gate's own second spelling of the manifest, both pins present, no token left, no build system; a second `m0 new` onto its output and an unusable name each exit 2 with the sentence whole and the tree untouched; then a sync that REFRESHES `m0` \u2014 the gate's own step, because uv serves a version it has cached and this wheel is rebuilt under one: a reverted layer fix once passed \u2014 with every file the wheel carries under `m0/` required byte-equal in the venv, and the literal `uv run m0 build` against the tree's wheel (stamped `0.1.0+tree`, which no index can serve, so the exact pin cannot resolve to a published one), `m0 test` green and then red with a failing test appended, the server started on the run's own port and stopped by pid with exit 0, `m0 doctor` as JSON green with the host's report inside, and the scaffold's own `smoke.sh` green on another port. `live` on the wire: the fragment at first paint with the pinned Datastar tag, two DIFFERING `datastar-patch-elements` frames for the root id inside five seconds, then a kick posted with that stream still open and, inside five seconds, a frame whose lowest bar stands above the highest bar the wave drew before it (N40's board word, which nothing else on the wire reads), the kick counted and no frame refused by the bus. `packaging/m0/tests/test_m0.py` holds the name rule and the manifest under the installed interpreter. The sync resolves online with every wheel cached, the dependency the job's own sync already has. `deploy/` is N31's. `poe sabotage-scaffold` (pre-release) breaks each rule from the template side, the wire rules with the template's own tests switched off"
    },
    {
      "id": "N28",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "The scaffold's templates are real source files that compile UNSUBSTITUTED against the tree: the application's name appears only where a compiler does not look \u2014 inside string literals, TOML and Markdown \u2014 so a change to the layer that breaks a template fails in the pull request that made it, not in the next person's first build. Every file under `templates/` is named by `new.py`'s manifest and every name there is a file; the two version tokens appear in `pyproject.toml` alone; and no other token of that shape exists, since a misspelled one would be written verbatim",
      "status": "verified",
      "evidence": "`Run unit tests` (every PR) \u2014 `poe check-templates`, inside `test-all`: `mojo build` of each template's `src/server.mojo` in place with the artifact thrown away, `mojo run` of each test file, then the manifest compared both ways with the files on disk and every token checked against the closed three"
    },
    {
      "id": "N29",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "An error a person may see is a fragment with its own status: `page_or_fragment(..., status=422)` with `text` left alone answers `422 Unprocessable Content` \u2014 the status's own RFC 9110 phrase from `reply.reason_phrase` \u2014 where it used to answer `422 OK`, the default having been the literal `OK`; an explicit `text` still wins. htmx 4 swaps every 4xx, so the scaffold's `views` template answers a bad form with the list, the message inside it under an alert role, and a missing item with a 404 fragment; `reply.problem` stays for routes no browser swaps",
      "status": "verified",
      "evidence": "`Smoke test the m0 scaffold` (every PR) \u2014 an empty title over a socket is `422 Unprocessable Content`, `text/html`, its body the bare fragment under its root id holding the alert, and a missing item is `404 Not Found` and the fragment; `test_fragment.mojo:test_a_status_alone_carries_its_own_reason_phrase` holds the phrase, the default and the override"
    },
    {
      "id": "N30",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "`m0 dev` is build-then-swap: it builds and serves `bin/server` with whatever followed a bare double dash, then polls `src/` and `pyproject.toml` by mtime with the standard library alone (D40 \u2014 no watcher dependency). On a change it builds WHILE the old server serves \u2014 a build is about ten seconds, too long to be down, and `m0 build` renames into place without touching the running inode (N24) \u2014 and a build that FAILS changes nothing: the old process keeps serving the last good build and `m0 dev` keeps watching. Only a build that succeeded ends the old process: SIGTERM by pid, a wait for that pid to exit (6 s, the host's drain plus one; SIGKILL after, named on stderr), how it ended said (`pid N exited 0`), and only then the new binary started, so there is never a second process on the port. Preflight is `checks.py`'s one list (N25). Ctrl-C, or SIGTERM, stops the server by pid and exits 0; a server that exits on its own is reported and the next edit starts it again",
      "status": "verified",
      "evidence": "`Smoke test m0 dev on the scaffold` (every PR) \u2014 a scaffolded `views` app outside the tree, the literal `uv run m0 dev` with a port handed through: the pid it names is the ONE pid listening, asked of the kernel (`lsof`, else `ss`) and not of `m0 dev`; one string literal edited and the port polled from the save \u2014 the OLD literal answered at least three times after it and before the new one is ever seen, then the old pid gone, reported `exited 0`, and a different single pid listening; a syntax error saved into the ENTRY file (mojo 1.1.0 builds an imported module holding a malformed `def` nothing calls with exit 0, measured) \u2014 the failed build reported, `m0 dev` alive, the same pid serving the same literal for two more seconds; the error removed and another literal changed \u2014 served by a third pid; SIGINT to `m0 dev` ALONE, not its process group, so the server sees no signal but the one `m0 dev` sends \u2014 the server gone, nothing listening, exit 0, and no SIGKILL anywhere in the run. `test_m0.py` holds what the wire cannot see: `stop` returning only once the pid is gone, the SIGKILL named for a server that ignores SIGTERM, and the snapshot seeing an edit, a new file, a deletion and `pyproject.toml` and nothing under `bin/`. `poe sabotage-scaffold` (pre-release) reverts eight rules of `dev.py` and requires this gate to fail for each"
    },
    {
      "id": "N31",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "`m0 image` builds what the scaffold writes under `deploy/`: `docker build -f deploy/Dockerfile` from the project root, tagged with the project directory's name unless told otherwise, then the image's own `/app/about.json` as the last line of stdout. The builder stage runs what a user runs \u2014 a frozen `uv sync` against the committed lock, then the release build of N24 \u2014 and the runtime stage carries `dist/` and nothing else: no interpreter, no toolchain, the binary as PID 1. `about.json` is measured by the image's last layer, which FAILS the build if an interpreter is anywhere in it, and records `cpu`: what the builder's release build SAID it compiled for, the platform's baseline unless a target CPU was named (`native` is exit 2). `m0 image` asks for docker and nothing else \u2014 of the one list (N25) only `project` \u2014 because the compiler runs in the builder; there is no docker check, since `m0 doctor` reads that list whole and a machine without docker is not a broken one. Docker missing or failing is exit 1 with its output untouched; what follows a bare double dash goes to `docker build` as it is. It does not deploy (D42)",
      "status": "verified",
      "evidence": "`Smoke test the m0 scaffold's deploy image` (every PR) \u2014 a scaffolded `views` app outside the tree; the Dockerfile and `.dockerignore` hashed before the build and after, so what is built is what `m0 new` wrote. The tree's wheel is `0.1.0+tree`, which no index serves: it is copied to `.wheels/` and the HOST's sync runs with a RELATIVE find-links, which uv records relatively (asserted in the lock) \u2014 an absolute one is recorded absolutely and a frozen sync in the builder could never be satisfied \u2014 and `.wheels/` reaches the builder in a base image handed to `docker build`, through the pass-through, as a named context that replaces `python:3.13-slim`. No docker on `PATH` is exit 1 naming docker, a flag docker refuses is exit 1 with docker's words and an empty stdout, `native` is 2. Then the user's `uv run m0 image`: `about.json` names this app, its version, the daemon's architecture, `\"python\":false` and the BASELINE `cpu` (`x86-64-v2` on CI's runner, the first x86-64 build of the scaffold's image); the BUILDER stage's installed `m0/` byte-equal to the wheel under test, file by file \u2014 the layer cache is uv's version cache again, and the gate does not take the base image's changed digest on trust; run with a published port: `/health`, `/proc/1/cmdline` exactly `/app/server`, the file in the image equal to the line printed, no `python*` or `libpython*` anywhere as root, and N27's whole `views` wire \u2014 the 422 fragment included \u2014 through the port; `docker stop` exit 0 inside 10 s; and a second build with a target CPU named, whose `about.json` says so. `poe sabotage-scaffold` (pre-release) breaks eleven rules \u2014 in `image.py`, the baseline table, the Dockerfile, and one that plants another `m0` in the builder \u2014 and requires this gate to fail for each. Not claimed: that a published `m0` resolves in the builder, which no gate can ask before one exists; since 2026-09-25 the runtime stage also installs `RUNTIME_LIBS` (`libsqlite3-0`, the library `m0_sqlite` opens at run time; `libpq5` by one build argument), records it in `about.json`'s `libs`, and the gate asks the running container for `libsqlite3.so.0`"
    },
    {
      "id": "N32",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "`m0` is published by a workflow of its own, from tags of its own: `release-m0.yml` has ONE trigger, a pushed tag matching `m0-v*` \u2014 which `release.yml`'s `v*` does not match \u2014 builds the wheel with no local label (the variable every smoke builds with is unset, and the build does not go through the poe task that defaults it), refuses a wheel whose version carries one, is not the tag's, was cut from another commit or a dirty tree, or declares a dependency, scaffolds once from the built wheel and requires the pin it writes to be the version being published, and uploads through PyPI's trusted publishing from an environment of its own, `pypi-m0`, the only job granted an identity token, with the publish action pinned to the commit `release.yml` pins. Exercised once: `m0-v0.1.0`, pushed 2026-09-21, was its first run, green in both jobs at the first attempt",
      "status": "verified",
      "evidence": "`Check machine-sourced doc facts` (every PR) \u2014 `m0_release_problems` in `scripts/check_docs.py`, a pure function of the two workflows' text with comments stripped: the single trigger, the unset and the direct build, the local-label refusal, the tag comparison, one identity token, the one environment, the shared pin, and `release.yml`'s patterns tried against `m0-v0.1.0`. Its selftest reverts ten rules against the committed files and counts an unchanged mutation as MISSED. What this cannot show is that the workflow RUNS: only a tag does, a PyPI filename being burned for good, so docs/RELEASING.md orders every tag behind a local run of the build job's own checks \u2014 as it did the first, recorded there"
    },
    {
      "id": "N33",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "The Mojo stack's front door is a page CI executes: `packaging/m0/QUICKSTART.md` takes a reader from `uvx m0 new` through `uv sync`, `uv run m0 build`, the served application probed over a socket (a document, the bare fragment for an htmx 4 partial, a created item, a 422 that is still the fragment), `uv run m0 test`, `uv run m0 doctor`, and `uv run m0 dev` swapping in an edited string literal while the port keeps answering. The page's image step is display-only and is NOT run here \u2014 half of this gate's runners have no docker \u2014 and N31's gate builds that image from a scaffolded project. The page lives under `packaging/` because this workflow ignores `docs/**` and root-level Markdown, so a pull request that edits only the page still runs it",
      "status": "verified",
      "evidence": "`Execute the Mojo quickstart` (every PR) \u2014 `scripts/run_quickstart.py`, the mechanism M10 uses, given the tree's `0.1.0+tree` wheel: `uvx m0` is pointed at the wheel file, the page's `uv sync` refreshes m0 and is followed by the byte-equality check N27's gate uses (uv caches a version by name), and every block runs in a scratch directory on a stripped `PATH` \u2014 uv's directory and the system's \u2014 with this repository's venv unreachable, which the runner asks before the page's first line on every run (a `mojo`, an `m0` or a venv already reachable fails it; with the stripped environment reverted it fails naming the tree's own `mojo`), so the toolchain the page builds with is the one the page installed. The task pins the page's exact block counts, and a page on which neither substitution matched is refused rather than run against whatever the index serves. Both platforms. Sabotaged by hand before landing, recorded in the note: a wrong command, a wrong expected string on the wire, a lost fence tag, the edit step's old literal"
    },
    {
      "id": "N34",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A freshly scaffolded project's first build prints no compiler warning, for both templates. The templates compile UNSUBSTITUTED in N28's gate, where `__M0_APP__` opening a docstring passes the compiler's summary lint; substituted, a lowercase application name opened the summary and every new project's first build began with a warning. The name is backticked in both entry files",
      "status": "verified",
      "evidence": "`Smoke test the m0 scaffold` (every PR) \u2014 the output of the literal `uv run m0 build` in the written project is searched for `warning:`, per template; with the backticks removed from one template the gate fails naming the file and the lint. Found by N33's gate on its first run"
    },
    {
      "id": "N35",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "The Mojo stack's six pages restate lists that live in source, and each is held to it: their six URLs are the ones D45 made permanent and the site's page table serves them there; the host page's refusal table is `host_checks`' names in the order the host applies them, its flag table the flags in the host's own help text, and its list of m0's checks `checks.py`'s in order; the index's command table is the parser's subcommands, and no code block on any of the pages runs a subcommand m0 lacks; the quickstart's two loop times are the figures the scaffold's `AGENTS.md` ships in every project; the quickstart's tagged block counts are the ones N33's task pins, so a fence that loses its tag fails on a doc-only pull request; and all six pages are under the bare-figure rule, every figure on them sitting in a block that says where it was observed",
      "status": "verified",
      "evidence": "`Check machine-sourced doc facts` (every PR) \u2014 `mojo_pages_problems` in `scripts/check_docs.py`, a pure function of the pages, the site's table and six source files; its selftest reverts thirteen rules against the committed texts, in the source or on the page as the rule runs, with two controls, and counts an unchanged mutation as MISSED. What it cannot show is that a sentence ABOUT a check or a flag is true"
    },
    {
      "id": "N36",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A query string is built by the layer, not by hand: `Query` takes pairs in order and writes them on a path (`q.on(url_for(X))`), names and values percent-encoded as `url_for` encodes a segment \u2014 RFC 3986 unreserved bytes kept, every other BYTE `%XX`, a space as `%20` \u2014 so a value that is not UTF-8 is encoded rather than trapped on (G14) and the result never carries what `Datastar.swap` refuses in a URL; `add` skips an empty value, so a filter's URL is only what is set and one view has one address, and `add_empty` keeps the pair whose presence is the meaning. `url_for` encoded the path alone and the encoder was private, so the first application outside `apps/` with a GET filter wrote its own",
      "status": "verified",
      "evidence": "`test_router.mojo:test_a_query_is_built_encoded_and_only_from_what_is_set` (every PR) \u2014 its neighbours send the result back through `URI.parse` (`&`, `=`, `+`, `#`, `%` and a space survive), encode bytes that are not UTF-8, and hand a query of hostile request data to `Datastar.swap`"
    },
    {
      "id": "N37",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A swap that arrives at a view can move the address bar: `push=True` on `Fragment.swap`, `Fragment.el` and `Html.swap[V]` writes the swap and then `Vocabulary.push_url` on the same element \u2014 `hx-push-url=\"true\"` for `Htmx` \u2014 so a filtered list or a detail reached by a swap can be reloaded, bookmarked and gone back to, the restore being the `HX-Request-Type: full` request N22 already answers as a document. Two refusals, never a no-op: the LAYER refuses a push on any verb but `get`, a pushed URL being one the browser GETs on reload (htmx 4's `query` included, its parameters being in the body); and the trait's default refuses, which is `Datastar`'s answer by name, Datastar 1.0.4's free bundle having no history handling for a back button to meet (D46)",
      "status": "verified",
      "evidence": "`Smoke test the fragment notes app` (every PR) \u2014 the list's link to a note (expression tier) and the detail's link back (builder) each carry `hx-push-url=\"true\"`, nothing else in the list does, and the pushed URL asked for as an htmx 4 restore is the note inside a document; each push reverted alone fails the gate at its own line. `test_html.mojo` holds the bytes in both tiers and the three refusals"
    },
    {
      "id": "N38",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "A route that takes GET answers HEAD: a HEAD that no route registers for its path reaches the view a GET would, parameters, table and loop placement included, and the server drops the body and keeps the GET's `Content-Length` (RFC 9110 \u00a79.3.2). A route registered for HEAD itself wins over its table's GET whatever the order they were added in, a path with no GET is still a 405 for HEAD, and `Allow` names HEAD beside GET. The table matched methods exactly, so every view route, `/health` included, answered HEAD 405 with `Allow: GET, OPTIONS`, against \u00a79.1's rule that a server supporting GET supports HEAD; the first application outside `apps/` found it on its own deploy",
      "status": "verified",
      "evidence": "`test_views.mojo:test_head_reaches_the_get_view_in_either_table` (every PR) \u2014 a HEAD through `dispatch`, the stateless loop and the loop with the state, never to another method's view; `test_router.mojo` holds the precedence and `Allow`. On the wire, smoke-blobs sends HEAD to a read view and a loop route, each answered with its GET's status, length and type and no body and followed by a request on the same connection that must parse (`head_probe.py --twin`), and to the stream, which ends at its head and leaves no viewer (`head_probe.py --hold`, L27)"
    },
    {
      "id": "N39",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "The `m0` wheel carries the two storage packages, `m0_sqlite` and `m0_postgres`, as source trees beside the five, mapped from the manifest like them (`hatch_build.py`'s table and the smoke's second spelling of it), and an application uses either with no link flag: both open their C library at run time (O17 for libsqlite3; libpq from the start), so `m0 build` takes nothing and `m0 test`, which is `mojo run` and resolves nothing from its own image, opens a database like any other test",
      "status": "verified",
      "evidence": "`Smoke test the m0 wheel` (every PR) \u2014 the contents phase holds the seven trees to `git ls-files`, and the project the smoke builds carries a test that opens SQLite in memory through the installed `m0_sqlite` and must be green under `m0 test`, on both platforms"
    },
    {
      "id": "N40",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "The `live` scaffold keeps its kick count in SQLite across a restart: `store.mojo` is one row in a `counters` table; `LiveHandler.make` opens the connection once per worker, loop or pool thread, AFTER the fork (a SQLite connection must not cross one), with the schema created inside an IMMEDIATE transaction so two workers over one fresh file do not race; the kick view counts in the database inside its own request, so the count is a committed row by the time the 204 is answered, and `/stats` reads it back from the file \u2014 never a producer writing the board's word back on its next poll, which lost a kick posted just before SIGTERM; the board's word keeps kicks since start for the wave alone. `M0_DB` names the file, else `NAME.db` beside the binary, and the image points it under `/app/data` with a volume the deploy runbook names",
      "status": "verified",
      "evidence": "`Smoke test the m0 scaffold` (every PR) \u2014 the scaffold's own `smoke.sh` kicks, restarts the server on the run's own database file and polls `/stats` for the kick; the template's test opens the store in memory under `uv run m0 test`, with nothing linked; `sabotage-scaffold` reverts the view's write (caught at the wire's first `/stats`, which reads the database) and swaps the file for an in-memory store (counted, then forgotten: caught only by the restart). The board's word is the wave's half, and the wire holds it too: a kick posted with a stream open must lift every bar of a later frame above the highest bar before it, so `sabotage-scaffold`'s third kick rule, which drops the word, is caught there \u2014 against `/stats` alone, which reads the database, it passed whole (MISSED on the 2026-09-26 pre-release run). The producer-owned write was the first shape and CI's ubuntu leg found it: `{\"steps\":0,\"kicks\":0}` after the restart, the kick having landed inside the producer's 50 ms poll and SIGTERM before the next"
    },
    {
      "id": "N41",
      "section": "N",
      "category": "N. Writing an application in Mojo",
      "capability": "MAX is optional beside an application and pinned when present (D50): the wheel records `gated_max` from the root's `max` dependency group as it records `gated_mojo` from the root pin, refusing anything but one exact `max-core==X`; `max-gated` is a check in the ONE list after `mojo-gated`, passing when `max-core` is absent (naming the `uv add` that installs it), refusing with the sentence and the fix when another version is installed, passing when the gated one is; the doctor's JSON carries `gated_max` and `max`; the scaffold's `pyproject.toml` carries the `uv add` line with the gated version substituted (`__M0_MAX_VERSION__`) and its `AGENTS.md` the rules \u2014 the capture list, where `parallelize` belongs, threads not forked workers (E32)",
      "status": "verified",
      "evidence": "`Smoke test the m0 wheel` (every PR) \u2014 `gated_max` against the root's group; a stub `max-core` 9.9.9 beside the REAL mojo, so `mojo-gated` passes and the sentence is `max-gated`'s alone, from `m0 build`, `m0 test` and the doctor; the plain doctor saying MAX is optional when none is installed; the release recipe's bundling of the runtime is E32's gate, where MAX is installed"
    },
    {
      "id": "O1",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "`open` promises WAL and raises when it cannot deliver it: `PRAGMA journal_mode` answers with the mode actually in force, and a database that stays on a rollback journal \u2014 a temp or in-memory database, some network filesystems \u2014 is an error at open rather than reader/writer concurrency that silently is not there; and two processes opening one fresh file at the same moment both get it, because the switch is retried on `SQLITE_BUSY` within the busy timeout, which SQLite does not consult for that pragma",
      "status": "verified",
      "evidence": "`test_file.mojo:test_open_reports_wal_it_could_not_apply` (every PR) \u2014 the discarded row is the defect: `open` would promise WAL while delivering a lock that serializes, which surfaces later as inexplicable contention rather than as an error here; `test_two_processes_open_one_fresh_database` beside it forks twelve pairs that open one fresh file at once, and about one pair in two lost the race on an M4 before the retry (29 of 30 two-worker starts of `apps/datastar_todo` refused, seen the day the Mojo host stopped hiding a raising `make` behind a respawn, E25)"
    },
    {
      "id": "O2",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "A `Statement` outlives the `Connection` that prepared it, because every close path uses `sqlite3_close_v2`: Mojo destroys a value at its last use, so a connection whose last mention was `prepare()` is already closed while its statement is still stepping",
      "status": "verified",
      "evidence": "`test_sqlite.mojo:test_statement_outlives_its_connection` (every PR) \u2014 `sqlite3_close` would refuse the close and leave the handle open, or break the statement, depending on the path; `close_v2` finalizes as statements are released"
    },
    {
      "id": "O3",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "Error text is only trusted when `sqlite3_errcode` corroborates the code being described: a closed connection answers SQLITE_MISUSE to every question, so a true \"UNIQUE constraint failed\" must not be replaced by a false \"bad parameter or other API misuse\"",
      "status": "verified",
      "evidence": "`test_sqlite.mojo:test_step_error_stays_truthful_without_a_live_connection` (every PR) \u2014 the corroboration holds only because nothing calls `sqlite3_extended_result_codes`; turning those on makes step return 2067 where errcode still answers 19, and every message would degrade to the generic text"
    },
    {
      "id": "O4",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "`m0_array(?)` streams a Mojo `List` into SQL without copying it, and the borrow cannot outlive the data: an array is bindable only through the helpers that take it as an argument and finish the statement before returning",
      "status": "verified",
      "evidence": "`test_vtab.mojo:test_borrow_is_dropped_after_the_call` (every PR) \u2014 the C struct offsets the module depends on are asserted against the real sqlite3.h by extraction before any Mojo test runs, and moving any one of them is caught, both inside `poe test-all`"
    },
    {
      "id": "O5",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "An application persists in SQLite through a per-thread connection and comes back with its data after a kill and a restart, including the event log that makes SSE replay work across processes",
      "status": "verified",
      "evidence": "`Smoke test the Datastar todo demo` (every PR) \u2014 `apps/datastar_todo` is killed and restarted on the same database file, the struck-through todo is still in the list, and a client reconnecting with `Last-Event-ID` is caught up by the restarted process from the persisted log"
    },
    {
      "id": "O6",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "Binary results decode to what the server means: integers big-endian and sign-extended from their OWN width, IEEE 754 floats at both widths, `jsonb`'s version byte read rather than skipped, `uuid` rendered canonically, and timestamps counted in microseconds from 2000-01-01 rather than from the Unix epoch",
      "status": "verified",
      "evidence": "`test_wire.mojo:test_negative_integers_are_sign_extended_from_their_own_width` (every PR) \u2014 the table is hand-built bytes, so it needs no server; `int2`'s -1 read as unsigned is 65535, and a timestamp read against the wrong epoch is thirty years early and entirely plausible"
    },
    {
      "id": "O7",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "Connection defaults \u2014 among them TCP keepalive timings and `tcp_user_timeout`, so a dropped idle connection is noticed in about a minute rather than the OS's two hours \u2014 are merged, never appended, and every URL is redacted before it reaches an error, a log or the doctor: libpq takes the LAST occurrence of a repeated keyword, so appending a default silently overrides the caller's own",
      "status": "verified",
      "evidence": "`test_url.mojo:test_a_default_the_caller_set_is_not_overridden` (every PR) \u2014 the redaction arm is `test_url.mojo:test_a_password_in_the_authority_is_masked`, covering both places libpq lets a password hide, plus a table of hostile strings each asserting only that its secret is absent from the output \u2014 an unencoded `/`, `?`, `@` or `:` in the password, a query or key/value form libpq itself would refuse, quoted and spaced values, `sslpassword` and `oauth_client_secret` \u2014 because a string `redact` cannot parse comes back masked whole, and libpq's own message beside it is withheld, since libpq quotes what it cannot parse (measured: half the password on every listener retry, before the fix fifteen of those rows leaked). The read-only helper refuses to half-apply its promise rather than adding a second `options` keyword that would replace the caller's"
    },
    {
      "id": "O8",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "Every error carries its SQLSTATE in its message and `sqlstate()` recovers it, with the context before the code so appending can never make it unrecoverable; the two predicates an application branches on \u2014 retryable, connection lost \u2014 name their classes once",
      "status": "verified",
      "evidence": "`test_sqlstate.mojo:test_the_context_goes_before_the_code_not_after` (every PR) \u2014 the same ordering rule `m0-sqlite`'s `describe_in` keeps for its result code, and for the same reason: three call sites there once came to raise without one"
    },
    {
      "id": "O9",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "libpq is opened at run time rather than linked, so no binary in this repo carries a libpq dependency, and an absent library is one error naming every path tried; the function table survives being moved, which is what the whole `PgLib` shape exists for",
      "status": "verified",
      "evidence": "`Test m0-postgres against a real server` (every PR) \u2014 a `thin` pointer loaded from a handle is identical by address before and after a move, and calling it as `table.field()` from outside the struct that holds it jumps into unmapped memory while the same call from a method beside it answers correctly, so every entry point is private behind a wrapper; `test_lib.mojo` asserts the working shape in the position the broken one failed in, because a dangling call is a segmentation fault rather than an exception"
    },
    {
      "id": "O10",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "Parameters are bound, not interpolated: a value carrying a quote and a semicolon arrives whole, every binding method round-trips through a real column, and NULL stays distinguishable from an empty value \u2014 which it is not once both have length zero",
      "status": "verified",
      "evidence": "`Test m0-postgres against a real server` (every PR) \u2014 `test_postgres.mojo`, in a schema named for the connection's own backend pid so parallel runs cannot collide"
    },
    {
      "id": "O11",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "The same query in text and binary modes reads the same, so `binary` is a performance decision rather than a semantic one; a binary type with no decoder raises naming the way out instead of returning plausible bytes",
      "status": "verified",
      "evidence": "`Test m0-postgres against a real server` (every PR) \u2014 `numeric` is text-only on purpose: its binary form is a base-10000 digit vector with its own NaN encoding, and a wrong decoding of one is silently a different number"
    },
    {
      "id": "O12",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "A failed statement carries its SQLSTATE and the SQL, and leaves the connection usable: a constraint violation is reportable as one rather than as a 500, and the connection answers the next query",
      "status": "verified",
      "evidence": "`Test m0-postgres against a real server` (every PR) \u2014 a transaction left INERROR still reads as open, because it still holds its locks and only ROLLBACK ends it"
    },
    {
      "id": "O13",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "A read-only connection refuses a write, whatever the role could do: the belt to a read-only role's braces, so a mistake in the grants is an error on the connection that should not be writing rather than a write that succeeds",
      "status": "verified",
      "evidence": "`Test m0-postgres against a real server` (every PR) \u2014 SQLSTATE 25006, distinguishable from the privilege error a role-level refusal gives"
    },
    {
      "id": "O14",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "An identifier put into SQL text is quoted by the SERVER, against the connection's own encoding, not by doubling quotes by hand \u2014 which is what `LISTEN` and `DEALLOCATE` need, taking no parameters",
      "status": "verified",
      "evidence": "`Test m0-postgres against a real server` (every PR) \u2014 a channel name is frequently application input"
    },
    {
      "id": "O15",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "`LISTEN`/`NOTIFY` delivers with its channel, payload and notifying backend, nothing is delivered before it is sent or twice after, a reset restores every subscription, and a notification read during a statement's round trip is still delivered by the next drain even though `poll` never reports it \u2014 which is why the `--pg-listen` thread drains once after connecting and after every reset",
      "status": "verified",
      "evidence": "`Test m0-postgres against a real server` (every PR) \u2014 the reset arm is the one that is invisible when wrong: `PQreset` opens a new backend session listening to nothing, so a listener that reconnects without re-`LISTEN`ing runs forever delivering nothing and logging no error, which looks exactly like nobody publishing"
    },
    {
      "id": "O16",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "A `Result` outlives the `Connection` that produced it: Mojo destroys a connection at its last use, so `var rows = db.query(...)` with no later mention of `db` finishes the connection on that line, and every read and the final `PQclear` still answer \u2014 because libpq, once opened, is pinned for the life of the process, and a result holds the entry points it calls by value rather than through its connection's address",
      "status": "verified",
      "evidence": "`Test m0-postgres against a real server` (every PR) \u2014 measured before the fix as a segmentation fault on the first read, three runs out of three, in both shapes the test holds: a connection whose last mention is the query, and a result returned out of the helper that opened its connection. Each half of the fix is necessary on its own: pinned but still reading through the address, the read faults in `Result.text`; unpinned but holding the pointers by value, it faults too, and `test_lib.mojo` asserts that half without a server by calling a copied table after its `PgLib` is gone"
    },
    {
      "id": "O17",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "libsqlite3 is opened at run time rather than linked, so no binary in this repo carries a libsqlite3 dependency on its link line \u2014 `mojo run` works on Linux as it does on macOS, which is the shape `m0 test` takes, and `m0 build` needs no flag for it \u2014 and an absent library is one error naming every path tried, `M0_LIBSQLITE3` naming the file outright; a library that opens and is not libsqlite3, or one older than 3.20.0, is refused naming the first symbol it lacks, never aborted on; a library built with `SQLITE_THREADSAFE=0` is refused, since the package opens one connection per thread; and every entry point is checked before it is loaded, in one place, taking its name from the declaration it loads",
      "status": "verified",
      "evidence": "`test_lib.mojo:test_the_library_opens_and_meets_the_floor` (every PR) \u2014 the search path, the floor and the thread-safety flag; `test_a_symbol_the_library_lacks_is_an_error_naming_it`, `test_a_path_that_is_not_a_library_is_an_error_naming_it` (through `M0_LIBSQLITE3` too, which is what a `Connection` sees) and `test_a_library_that_is_not_libsqlite3_is_refused_by_symbol` beside it. `build-apps` (inside `poe test-all`) builds `apps/datastar_todo`, the package's one consumer, and fails if its binary names libsqlite3 in a `NEEDED` entry or a load command, which is where an `external_call` put back anywhere in the package would reappear"
    },
    {
      "id": "O18",
      "section": "O",
      "category": "O. Storage packages",
      "capability": "A `Statement` outlives every handle the library was opened through, not only the `Connection` that prepared it (O2): each connection holds its own library table (`SqliteLib`, the handle and its entry points in one struct) and each statement a COPY of the entry points it calls (`StmtLib`), which is sound only because the library is pinned for the life of the process \u2014 re-opened `RTLD_NODELETE` at construction, so no `dlclose` unmaps it; the virtual-table callbacks hold a copy the same way, in the buffer SQLite owns, which is how a callback invoked from C reaches the library without a global",
      "status": "verified",
      "evidence": "`test_lib.mojo:test_a_statement_outlives_every_handle_of_the_library` (every PR) \u2014 the connection is closed and dropped, the only handle with it, and the statement then steps and reads through its copies. The sabotage is the pin removed from `SqliteLib.__init__`: measured 2026-09-25 on Linux, where nothing else has the library mapped, the same test then dies of a segmentation fault at its first `step` rather than failing (docs/notes/sqlite-at-run-time.md)"
    }
  ]
}
