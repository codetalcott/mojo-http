# Mojo build artifacts
*.mojoc
*.mojopkg
*.o
*.so
*.dylib

# uv (uv.lock IS tracked — it pins the Mojo toolchain)
.venv/

# IDE — the example is tracked; real settings hold machine-absolute paths.
.vscode/*
!.vscode/settings.example.json
.idea/

# OS
.DS_Store

__pycache__/

# Compiled app binaries (rebuild with `mojo build`)
apps/hello/hello
# The serve CLI (rebuild with `uv run poe build-serve`)
bin/

# warning ratchet input (CI tees build output here; see scripts/warning_ratchet.py)
compile.log

# smoke test output
hello.log
counter.log
counter2.log
django.log
django2.log
notes.log
todo.log
ws.log
chat.log
chat2.log
sse.txt
expected.bin
got.bin
rt.log
rt2.log
rt3.log
ws1.log
ws2.log
flask.log
wsgi.log
serve.log
threads.log
asgi.log
fasthtml.log
hybrid.log
hybrid2.log
django3.log
shutdown1.log
shutdown2.log
shutdown3.log
pool.log
reload.log
rt4.log
rt5.log
rt6.log
stream.log
ht.log
canary.log
# Per-connection probe output from the multi-worker and realtime smokes:
# the index is a loop variable, so these are globs rather than names.
mw_hdr_*.txt
mw_sse_*.txt
todo_*.txt
slow*.txt
serve_*.txt
big_app.bin
big_live.bin
fanout.log
djasgi.log
dj.jar
.venv-pinned/
py-canary.log
probe.out
dist/
.sesskey

# Wheel build output. _bin/_lib/licenses under the packaging project are
# STAGED by `poe stage-wheel` from artifacts built elsewhere in the tree, so
# they are build output rather than sources. Hatchling excludes VCS-ignored
# files by default, which is why packaging/m0serve/pyproject.toml has to name
# them under `artifacts` -- see the comment there.
packaging/m0serve/src/m0serve/_bin/
packaging/m0serve/src/m0serve/_lib/
packaging/m0serve/src/m0serve/licenses/
packaging/m0serve/licenses/
packaging/m0serve/dist/
